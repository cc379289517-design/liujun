# SPAD 摄影助理自动派单系统 — 当前运行逻辑与规范

> 最后更新：2026-06-22（收录移动端适配、身份一致性、PWA/App 与百人生产化路线）
> 技术栈：Next.js 16.2.1 / React 19 / SQLite / Prisma 7 / Socket.io 4 / TailwindCSS 4
> 文档地位：本文件是整个站点唯一的当前运行逻辑、业务规范和重要说明入口；每次确认新的业务逻辑，都应更新本文件。

---

## 目录

0. [文档地位与维护公约](#0-文档地位与维护公约)
1. [数据模型](#1-数据模型)
2. [角色与状态体系](#2-角色与状态体系)
3. [自动派单调度器](#3-自动派单调度器)
4. [任务生命周期](#4-任务生命周期)
5. [API 接口清单](#5-api-接口清单)
6. [页面功能](#6-页面功能)
7. [系统配置参数](#7-系统配置参数)
8. [实时通信](#8-实时通信)
9. [业务规则分类沉淀](#9-业务规则分类沉淀)
10. [文档维护与后续补齐](#10-文档维护与后续补齐)

---

## 0. 文档地位与维护公约

### 0.1 与「程序实际运行」的关系

- **Next.js 应用在运行时执行的是编译后的 TypeScript/JavaScript**，读取数据库并通过 API、调度器函数改变状态。**不会**在请求链路中解析或执行本 Markdown 文件。
- 因此：**业务规则的真实载体仍是代码与 `schema.prisma`**；本文件定位为 **人机共读的唯一当前规范入口（Single Source of Truth for intent）**：描述系统**应当**如何表现，供产品、开发、测试与 AI 助手对齐。
- `tasks/project-operation-logic-*.md` 只作为历史归档和决策证据保留；若与本文档冲突，以本文档当前口径为准。
- `tasks/todo.md` 只记录任务计划和评审；`tasks/lessons.md` 只记录经验教训；二者不作为当前业务规则入口。
- 若需「文档驱动运行」（运行时装载规则文件），需要单独建设规则引擎或 DSL，**不在当前仓库实现范围内**。

### 0.2 变更流程约定（推荐作为团队/个人纪律）

1. **讨论或实现任何业务逻辑变更时**：同步修改本文件对应章节（调度 / API / 前端展示 / 配置），或先改文档再改代码、或改代码后立即补文档，**二者不得长期脱节**。
2. **Pull Request / 自测检查**：可自问「`摄影助理自动派单系统.md` 是否已反映本次行为？」；可选地在 CI 中加「触及 `scheduler.ts` 或 `api/tasks` 时提醒更新文档」等非阻断提示。
3. **发现文档与代码不一致**：以**代码当前行为**为排查起点，然后**以文档更新为交付的一部分**（除非明确认定代码为 bug 需回滚）。

### 0.2a 四人协作分工

| 身份 | 负责内容 | 边界 |
|------|----------|------|
| 项目经理老王 | 主控 / 分派 / 汇总 / 验收 | 负责最终合并和验收 |
| UI 相关小李 | UI / UX / 移动端 / PWA 视觉交互 | 不改核心派单逻辑 |
| 逻辑相关小刘 | 业务逻辑 / API / 文档 / 测试 | 不随便重做 UI；不主责部署 |
| 优化中心小张 | 性能 / 并发 / 数据库 / 架构优化 / 部署 / 生产环境 | 不改业务规则 |

- 当前主控会话身份为 **项目经理老王**：负责拆任务、派发、汇总、验收和最终合并判断。
- 小刘负责业务逻辑、API、文档和测试；部署、生产环境、发布流程、服务器、域名 HTTPS、进程管理、数据库生产化迁移等事项交给小张。
- 小张负责性能、并发、数据库、架构优化、部署和生产环境；不得擅自改变业务规则，涉及业务口径时先反馈给老王或小刘。
- 小李负责 UI/UX、移动端、PWA 视觉交互；不得擅自修改核心派单、任务状态、调度和数据库规则。
- 各执行会话收尾动作保持克制：完成职责内代码或方案；跑自己负责范围内的验证；更新当前进度 `tasks/todo.md`；必要时更新长期规则 `摄影助理自动派单系统.md` 或经验教训 `tasks/lessons.md`；最后向老王汇报完成情况。
- 跨角色修改需先说明原因和影响，并由老王最终验收；默认只在老王主控会话中统一 commit。

### 0.3 本文档结构说明

- **第 1–4 节**：数据与状态、生命周期。
- **第 3 节**：调度器、插单、熨烫等待与自动均衡。
- **第 3.8 节**：有效执行时长字段与 `taskEffectiveTime` 工具函数（与 PATCH/完成逻辑一致）。
- **第 5 节**：HTTP API 全量索引与 `PATCH` 各 action 细则。
- **第 6–8 节**：页面、配置、Socket。
- **第 9 节**：按大类别沉淀已确认业务规则；新增逻辑应优先放入对应类别小节，日期只作为历史归档索引。
- **第 9.9 节**：移动端、身份一致性、PWA/App 封装、部署和百人并发生产化路线。
- **第 10 节**：仍偏宏观或未细化的能力清单，便于迭代补齐。

---

## 1. 数据模型

### 1.1 Building（楼座）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Int (PK) | 自增主键 |
| name | String | 楼座名称，如"1号楼" |
| floorPlanUrl | String? | 楼层平面图 URL |
| cropX/Y/W/H | Float? | 裁剪区域坐标（百分比 0-100） |
| extraVenues | String? | 额外场地 JSON，如 `[{name,x,y,type}]` |

### 1.2 Room（房间）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Int (PK) | 自增主键 |
| buildingId | Int (FK) | 所属楼座 |
| roomNumber | String | 房间号 |
| floor | Int | 楼层，默认 1 |
| xPosition | Float | 在平面图上的 X 坐标（0-100%） |
| yPosition | Float | 在平面图上的 Y 坐标（0-100%） |
| fenceRadius | Float | 地理围栏半径（米），默认 5 |

- 唯一约束：`(buildingId, roomNumber)`

### 1.3 Profile（用户）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (PK) | UUID |
| employeeId | String? (unique) | 工号 |
| name | String | 姓名 |
| avatar | String? | 头像 |
| role | Role 枚举 | 角色 |
| department | String? | 部门 |
| group | String? | 组别 |
| buildingId | Int (FK) | 所属楼座 |
| currentRoom | String? | 当前位置房间号 |
| status | ProfileStatus 枚举 | 工作状态，默认 idle |
| subStatus | String? | 微标签：上厕所/请假/身体不适 |
| onlineStatus | OnlineStatus 枚举 | 在线状态，默认 online |
| isOnline | Boolean | 历史字段（已弃用，调度器使用 onlineStatus） |
| activeBuildingId | Int? (FK) | 助理当前服务楼座/区域；独立于人员管理登记楼座 |
| activeRoom | String? | 助理当前服务房间/公共区域；独立于登记办公室 |

### 1.4 TaskCategory（任务类型）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Int (PK) | 自增主键 |
| name | String | 类型名称，如"短时手持"、"服装穿戴" |
| priorityLevel | Int | 优先级 P1-P6（1=紧急，数字越大优先级越低） |
| minDuration | Int | 最短时长（分钟） |
| maxDuration | Int | 最长时长（分钟） |
| estDuration | Int | 预估时长，默认 10 |
| hexColor | String | 状态环颜色，默认 #f97316 |
| sortRank | Int | 排序权重 |
| canBeInterrupted | Boolean | 执行中是否允许被更紧急单插断（默认 true） |
| maxInterruptMinutes | Int? | 该类型允许的「离场」上限（分钟）；与全局 `interruption_max` 取较小值；null 仅用全局 |

### 1.5 BookingTask（任务预约）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (PK) | UUID |
| photographerId | String (FK) | 发起摄影师 |
| assistantId | String? (FK) | 分配的助理（null=未分配） |
| roomNumber | String | 任务房间号 |
| categoryId | Int (FK) | 任务类型 |
| priority | Int | 动态优先级（数值越小越紧急；与 `PRIORITY` 常量一致，常见 1–5） |
| escalatedFromPriority | Int? | 第一次提权前的原始优先级，用于显示 `P初始→P当前` |
| status | TaskStatus 枚举 | 任务状态，默认 waiting |
| isLocked | Boolean | 是否锁定（锁定后不可被插单） |
| lockReason | String? | 锁定原因 |
| isSpecified | Boolean | 是否指定助理 |
| estEndTime | DateTime? | 预计结束时间 |
| note | String? | 备注 |
| locationBuildingId | Int? (FK) | 任务发生楼座；优先于摄影师登记楼座参与派发、统计和队列判断 |
| publisherFeedback | String? | 摄影师反馈：`like` / `dislike` / null |
| parentTaskId | String? (FK) | 被插单时的父任务 |
| startedAt | DateTime? | 首次开始执行时间（多次 start 时保留首次合法值） |
| completedAt | DateTime? | 完成时间 |
| pausedAt | DateTime? | 最近一次进入 paused 的时间 |
| escalatedAt | DateTime? | 上次提权时间 |
| effectiveWorkSeconds | Int | 已累计的**有效执行秒数**（仅统计 status=executing 的片段） |
| workSegmentStartedAt | DateTime? | 当前 executing 片段起点；pause/complete/setStatus 时经 `flushExecutingSegment` 结算 |
| ironingStage | String | 熨烫阶段：`none / waiting_machine / notified / using` |

- 索引：`(status, priority)`, `(assistantId, status)`, `(photographerId)`

### 1.6 SystemConfig（系统配置）

| 字段 | 类型 | 说明 |
|------|------|------|
| key | String (PK) | 配置键 |
| value | String | 配置值 |
| label | String? | 可读标签 |

---

## 2. 角色与状态体系

### 2.1 角色（Role）

| 角色 | 英文 | 权限 |
|------|------|------|
| 摄影师 | photographer | 创建任务、查看任务列表、管理自己的任务 |
| 助理 | assistant | 接收派发任务、执行、更新状态 |
| 助理组长 | assistant_leader | 助理权限 + 审批、管理监督 |
| 管理员 | admin | 全系统管理 |

### 2.2 工作状态（ProfileStatus）

```
idle（空闲）──→ assigned（已分配）──→ executing（执行中）──→ idle
                                       │
                                       ├──→ busy（暂停中）──→ executing
                                       │
                                       └──→ finishing（即将结束）
```

| 状态 | 颜色 | 说明 |
|------|------|------|
| idle | #22c55e 绿色 | 空闲可接单 |
| assigned | #3b82f6 蓝色 | 已派单待就位 |
| executing | #f97316 橙色 | 正在执行任务 |
| busy | #fdba74 浅橙色 | 任务暂停中 |
| finishing | #86efac 浅绿色 | 距结束 < 2 分钟（数据库/同步仍可能存在；**工作台 Dock 与身份切换列表的状态点不再使用浅绿色**，见下 2.2.1） |

### 2.2.1 工作台助理状态点（右侧 Dock、身份切换弹窗列表）

与 `AssistantDock.tsx` 中 `assistantDockDotColor()`、`DOCK_DOT` 一致，**状态点颜色**按下列优先级判定（头像右下角圆点）：

| 颜色 | 含义 | 判定 |
|------|------|------|
| 绿色 `#22c55e` | 空闲中 | `status === idle` 且非下线/休假 |
| 蓝色 `#3b82f6` | 待就位 | `status === assigned` |
| 橙黄色 `#f97316` | 进行中 / 在忙 | `status === executing` 或 `busy`；若后台为 `finishing` 亦按进行中显示为橙色（**已取消 Dock 上的浅绿色「快结束」状态点**） |
| 红色 `#dc2626` | 已超时 | `executingOvertimeMin` 或 `pausedOvertimeMin` 有值（有效工时超过类别时段上限），且非下线/休假 |
| 灰色 `#9ca3af` | 已下线 | `onlineStatus === offline` 或 `on_break`（头像灰显 + 灰点；悬停文案仍区分「下线」与「休假」） |

图例（Dock 底部）与上表一致：空闲中 / 待就位 / 进行中 / 已超时 / 已下线。

### 2.3 在线状态（OnlineStatus）

| 状态 | 说明 |
|------|------|
| online | 在线可用 |
| offline | 离线（不接受派单） |
| on_break | 休息中 |

### 2.4 微标签（subStatus）

- `上厕所` / `请假` / `身体不适` / `用餐中`
- 设置了微标签的助理不参与自动派单

### 2.5 可派发候选助理（参与自动派单的前提）

普通新任务和未分配 waiting 任务使用统一“可派发候选”口径，不能只看 `profile.status=idle`。

必须**同时满足**以下所有条件：

1. `role` = `assistant` 或 `assistant_leader`
2. `onlineStatus` = `online`（在线）
3. `subStatus` = `null`（无微标签）
4. 与任务在**同一当前工作楼座/区域**（优先 `activeBuildingId`，缺失时回退 `buildingId`）
5. `status` 允许为 `idle` 或 `assigned`
6. 没有真实占用工作：不存在普通 `waiting` 待就位、`executing`、`paused` 主任务，也不存在活跃协作任务
7. 如果助理身上只有未开始熨烫等待，则仍视为可派发候选，可继续承接普通新任务

**未开始熨烫等待**包括：

- `ironingStage=waiting_machine`
- `ironingStage=notified`
- 历史/普通待就位口径里的 `waiting + 熨烫任务 + 已分配 + ironingStage=none`

这些状态表示“助理正在等待机器或已获得短暂机器使用权”，不等于正在工作；只有熨烫任务进入 `using/executing` 后，才同时占用助理和熨烫机。

多人协作熨烫必须按“机器槽位”计算容量，而不是按任务条数计算：

- 一台熨烫机同一时间只能给一名助理使用。
- 熨烫任务所需机器数 = 主助理 + 未退出、未完成的协作者去重人数，至少 1 台。
- 2 人协作熨烫占用 2 台熨烫机；3 人协作熨烫占用 3 台熨烫机。
- `paused` 的熨烫参与者仍视为占用机器，避免暂停期间机器被重复分配。
- `notified` 准备熨烫属于临时机器使用权预占，也要按同一机器槽位口径扣减。

---

## 3. 自动派单调度器

> 核心文件：`src/lib/scheduler.ts`

### 3.1 派单函数一览

| 函数 | 作用 |
|------|------|
| `dispatchableAssistantsForBuilding(buildingId)` / `dispatchableAssistantIds(...)` | 统一可派发候选口径：在线、无微标签、同楼座，且没有真实占用工作；仅有未开始熨烫等待仍可派发 |
| `assignTask(taskId, buildingId?)` | 为单个任务找同楼座可派发助理并分配；会处理未开始熨烫等待不占用助理的情况 |
| `autoClaimWaitingTask(assistantId)` | 助理变为可派发时，自动认领最高优先级的未分配任务 |
| `runTaskMaintenance({ force? })` | 统一任务维护入口：每日清理、状态同步、动态提权和全局扫描；写入型操作或显式扫描调用，高频读取不调用 |
| `sweepWaitingTasks()` | 全局扫描：释放摄影师限流队列、处理待就位转派、推进熨烫队列、均衡熨烫等待、释放未选普通待就位任务，再匹配未分配任务 |
| `reassignOverdueStandbyTasks()` | 普通待就位和 `notified` 准备熨烫超时后，更换为同楼座可派发助理 |
| `sweepIroningMachineQueue()` | 扫描熨烫机容量和等待队列，把可开始的熨烫任务推进到 `notified` |
| `ironingMachineSlotsForTask(task)` | 统一计算多人协作熨烫所需机器槽位：主助理 + 未退出/未完成协作者，暂停仍占用 |
| `balanceIroningWaitAssignments()` | 同楼座有可派发助理时，把集中在同一人身上的额外未开始熨烫等待自动均衡出去 |
| `releaseUnselectedStandbyTasks()` | 助理已开始执行/暂停另一条真实任务时，把其未选择的普通已分配待就位任务释放给同楼座空闲助理 |
| `canInterrupt(assistantId, priority)` | 判断是否可以插单（仅 P1 可插 P4 未锁定） |
| `interruptAssistant(assistantId, newTaskId)` | 执行插单：暂停当前任务，分配新任务 |
| `escalatePriorities()` | 动态提权：等待超时的任务自动升级优先级 |
| `completeTask(taskId)` | 完成任务并释放助理，恢复被暂停的父任务 |
| `cleanupStaleTasks()` | 每日清理：昨天及更早的未完成任务自动标记完成 |
| `syncProfileStatus()` | 状态同步：以任务表为真实来源修正助理状态不一致 |
| `interruptWaitingPreempt(buildingId, newTaskId, newPriority)` | 待就位插单：无可派发助理时把更紧急的新单挂到「已 assigned、原单仍在 waiting」的助理上，见 [3.4](#34-插单与让行完整说明) |
| `interruptExecutingPreempt(buildingId, newTaskId, newPriority, leaveUpperMinutes)` | 执行中插单：新建任务和公共队列 sweep 共用同一套候选过滤、离场 cap 和保护规则 |
| `effectiveInterruptLeaveCapMinutes(global, typeMax)` | 执行中插单离场 cap：全局与类型 `maxInterruptMinutes` 取较小值 |
| `buildP1InterruptCandidateOrderFromFiltered(...)` | 同楼座可插断候选的排序：受 `p1_interrupt_dispatch_mode` 与楼座轮询指针影响 |
| `getSchedulerRuntimeConfig()` | 从 `SystemConfig` 读取提权/预警/中断上限，失败则回退代码默认 |

### 3.2 sweepWaitingTasks() 算法

调度器对 `sweepWaitingTasks()` 使用 **单例防抖**（`_sweepRunning`），防止并发重入。

**当前执行顺序**

```
0. runTaskMaintenance(): cleanupStaleTasks() → reconcileCompletedParticipantTasks() → syncProfileStatus() → escalatePriorities() → sweepWaitingTasks()
1. releaseAllEligiblePhotographerLimitQueuedTasks()
2. reassignOverdueStandbyTasks()
3. sweepIroningMachineQueue()
4. balanceIroningWaitAssignments()
5. releaseUnselectedStandbyTasks()
6. 未分配 waiting ↔ 同楼座可派发候选助理
7. 仍为孤儿 waiting → interruptWaitingPreempt()
8. 待就位让行失败且非熨烫类新单 → interruptExecutingPreempt()
```

`reconcileCompletedParticipantTasks()` 用于修复任务本体和参与记录的聚合不一致：当任务仍为未完成状态，但全部当前参与记录均已 `completed` 时，先把任务本体收敛为 `completed`，再同步人员状态，避免公共队列显示“已分配待就位”而助理状态显示空闲。

**维护触发边界**

- `GET /api/tasks` 是高频读取接口，原则上保持纯读取，不在每次列表查询时触发全局清理、状态同步、动态提权或派单扫描。
- 清理、状态同步、动态提权和扫队列统一通过 `runTaskMaintenance()` 执行。
- 写入型接口在改变任务或人员状态后，应按需触发 `runTaskMaintenance()`，确保创建任务、开始/完成任务、取消任务、人员状态变化、转派确认后仍能继续自动派单。
- `/api/tasks/sweep` 是显式维护入口，应调用 `runTaskMaintenance()`，并需要轻量节流或运行中保护，避免多个页面同时触发重复扫描。
- 发单接口在判断摄影师发布上限前应先执行必要维护/每日清理，避免跨天遗留的未完成任务造成“页面空但不能发单”的假阻塞。

**未分配 waiting 的直接派发**

1. 查找 `assistantId=null`、`status=waiting`、非摄影师限流锁定、`ironingStage=none` 的任务
2. 按楼座构建可派发助理队列：在线、无微标签，`status=idle/assigned`，且没有真实占用工作
3. 按 `priority` 升序、`createdAt` 升序遍历任务
4. 同楼座 dequeue 一名助理，事务写入 `assistantId`，创建/更新 primary 参与记录，并把 `profile.status` 置为 `assigned`
5. 记录该楼座空闲派发轮换指针，避免连续反复派给同一人

**仍为孤儿 waiting → 待就位让行 / 执行中插单（与 `POST /api/tasks` 一致）**

- 直接派发结束后，若仍有 `waiting && assistantId=null` 的任务，**逐条**对任务所在楼座调用 `interruptWaitingPreempt(buildingId, taskId, task.priority, leaveUpperMinutes)`。
- 若待就位让行失败，且新任务不是熨烫类任务，则继续调用 `interruptExecutingPreempt(buildingId, taskId, task.priority, leaveUpperMinutes)`，让公共队列中等待的高优先任务也能在后续 sweep 中插入正在执行的低优先任务。
- 执行中插单继续复用同一套保护：新任务必须更紧急、当前执行任务类型允许被插单、离场上限满足、多人协作/熨烫排队压力/锁定/已有插单任务均不可被插。
- 目的：修复「全楼无 idle 时 sweep 在以前会直接结束、后续公共队列任务不会继续尝试插单」的问题；保证 **写入后扫描/显式扫描** 与 **创建任务** 行为一致。

返回值为本次释放、转派、推进、均衡、直接分配、待就位让行和执行中插单的合计成功次数。

### 3.3 派单触发时机

| 触发场景 | 位置 |
|----------|------|
| 创建新任务前 | `POST /api/tasks` 先强制 `runTaskMaintenance({ force: true })`，再计算摄影师发布上限 |
| 创建新任务后 | `POST /api/tasks` 返回前调用 `runTaskMaintenance()`，处理释放、提权和后续派发 |
| 删除任务后（单个/批量） | `DELETE /api/tasks` / `DELETE /api/tasks/[id]` 调用 `runTaskMaintenance({ force: true })` |
| 任务状态变化后 | `PATCH /api/tasks/[id]` 调用 `runTaskMaintenance()` 或 `runTaskMaintenance({ force: true })` |
| 任务完成后 | `completeTask()` 释放/恢复后触发同步和扫描 |
| 助理变为可派发状态时 | `PATCH /api/profiles/[id]` 调用 `runTaskMaintenance({ force: true })` |
| 转派通知确认后 | `PATCH /api/reassignment-notices/[id]` 调用 `runTaskMaintenance({ force: true })` |
| 手动触发 | `POST /api/tasks/sweep` 调用 `runTaskMaintenance()` |
| 高频查询任务列表 | `GET /api/tasks` 只读，不触发维护和派单 |

### 3.4 插单与「让行」完整说明

代码里与「插单」相关的有两条路径：**待就位插单**（原任务尚未开始执行）与 **执行中插单**（原任务已在 executing）。二者都使用 `parentTaskId` 指向被让行的原任务，但 **数据库里原 waiting 任务不会被自动改成 paused**；执行中路径则依赖助理在工作台 **手动暂停** 后再开始新单。

#### 3.4.1 待就位插单 — `interruptWaitingPreempt`

**触发**：`POST /api/tasks` 在非指定助理模式下，`assignTask` 找不到同楼座可派发助理时；以及 `sweepWaitingTasks()` 对仍无助理的 waiting 任务。

**候选助理**：同楼座 `status=assigned`、在线、无 subStatus；且存在一条 **`waiting` + `parentTaskId=null` + 未锁定** 的「待就位」任务（按 priority **降序**、createdAt 升序取该助理当前让行目标——即优先抢「当前待就位单优先数最大（最低优先）」的助理）。

**排除**：若该助理已存在另一条 `waiting` 且 `parentTaskId` 指向上述父任务（已有插单子任务），则 **不再** 作为抢占对象，避免同一父任务挂多条插单。

**条件**：新任务必须比该助理当前这条 waiting **更紧急**：`waitingTask.priority > newPriority`（数值更大 = 优先数更低）。

**写入**：`newTask.assistantId = 选中助理`，`newTask.parentTaskId = 那条 waiting 的 id`；**父任务保持 waiting + 原 assistantId**（让行等待）。

**排序**：在过滤后的候选集合上调用 `buildP1InterruptCandidateOrderFromFiltered`（同 [3.4.3](#343-p1-插单候选顺序与楼座轮询)），成功后 `recordP1InterruptRoundRobin` 更新楼座轮询指针。

#### 3.4.2 执行中插单 — `interruptExecutingPreempt` + `canInterrupt` + `interruptAssistant`

**触发**：`POST /api/tasks` 在待就位插单也失败之后，以及 `sweepWaitingTasks()` 处理公共队列未分配 waiting 任务且待就位让行失败之后，扫描同楼座 `executing/busy` 的助理及其 **executing** 任务。

**canInterrupt(assistantId, newPriority)** 条件摘要：

- 必须存在该助理的 **executing** 任务，且类型 **`canBeInterrupted === true`**；
- **执行中任务若为 P1（priority 最小档）则不可被插断**（代码：`currentTask.priority <= P1` 则 false）；
- **新单须更紧急**：`newPriority < currentTask.priority`（同级或更松不可插）；
- 当前任务 **未锁定**。

**共用 helper 额外过滤**（与 cap 相关）：

- 新任务「离场」上界：`category.maxDuration > 0` 时用 `maxDuration`，否则用 `estDuration`；
- 当前任务允许离场：`effectiveInterruptLeaveCapMinutes(全局 interruption_max, category.maxInterruptMinutes)`；
- 仅当 **新单离场 ≤ cap** 时保留为候选。
- 熨烫机排队保护：同楼座存在 `waiting_machine/notified` 熨烫等待压力时，正在执行熨烫任务的助理不进入执行中插单候选；`canInterrupt()` 也会返回 false，避免其他入口绕过。
- 外模跟拍协助保护：「外模跟拍协助」虽然发布等级为 P6，但执行中属于全天跟拍工作模式，不进入执行中插单候选；`canInterrupt()` 和 `interruptAssistant()` 都会兜底返回 false。

**interruptAssistant** 行为：**不**自动把旧任务改为 paused；仅给新任务写入 `assistantId` 与 `parentTaskId = 当前 executing 的 id`。助理需在界面暂停当前单后再开始新单（与旧文档「自动暂停」表述不同，以代码为准）。

#### 3.4.3 P1 插单候选顺序与楼座轮询

- 配置键 **`p1_interrupt_dispatch_mode`**（管理后台任务逻辑 Tab）：
  - `priority_tier_rr`（默认）：候选按 **当前执行任务 priority 降序**（先 P5 档再 P4…），**同一优先档内**按助理 id 排序后轮询（`orderP1InterruptCandidates`）。
  - `flat_round_robin`：全体候选按 `assistantId` 排序后整表轮询（`orderP1InterruptFlatRoundRobin`）。
- 每楼座轮询指针：`SystemConfig` 键 `p1_interrupt_rr_b{buildingId}`，在每次成功待就位插单或执行中插单后更新。

#### 3.4.4 完成子任务后恢复父单 — `completeTask`

- 若完成的任务 **`parentTaskId` 有值**（无论是待就位插单还是执行中插单产生）：  
  子任务标记完成；**父任务**设为 `waiting`、`pausedAt=null`；助理 **保持 `assigned`**（需重新就位继续父单或后续流程）。
- 若无 `parentTaskId` 但存在同助理 **paused** 任务：完成当前单后把 **paused → waiting**，助理 `assigned`。
- 否则：助理 **idle**，并调用 `sweepWaitingTasks()`。

### 3.5 动态提权

```
规则：等待中的任务超过 30 分钟（可配置），优先级自动升一级
  P4 → P3 → P2 → P1
记录 escalatedAt 防止重复提权
```

- 动态提权覆盖所有尚未开始的 `waiting` 任务：未分配任务和已分配但尚未就位任务都参与。
- 已分配待就位任务的等待起点优先使用主助理参与记录 `joinedAt`；缺失时回退到任务 `createdAt`。
- 摄影师发布上限锁住的任务不参与普通提权，必须等发布名额释放后再进入正常队列。

### 3.6 每日清理（cleanupStaleTasks）

- 每天首次请求时执行（日期标记防重复）
- 将**昨天及更早**的 `waiting` / `paused` / `executing` 任务标记为 `completed`
- 释放关联助理（仅当该助理今天没有活跃任务时）
- 摄影师创建新任务时，必须先执行任务维护/每日清理，再计算摄影师发布上限，避免昨天遗留未完成任务在手机端今天列表不可见、但仍挡住今天发单。

### 3.7 状态同步（syncProfileStatus）

- 以 BookingTask 表为真实来源
- 对比每个助理的实际任务与 profile.status
- 计算活跃任务时排除未开始熨烫等待；只有真实普通待就位、执行中、暂停中或活跃协作任务才会占用助理状态
- 自动修正不一致：
  - 有 executing 任务 → status = executing
  - 有 waiting 任务 → status = assigned
  - 有 paused 任务 → status = busy
  - 无活跃任务 → status = idle

### 3.8 有效执行时长（`taskEffectiveTime`）

> 实现：`src/lib/taskEffectiveTime.ts`。与「墙钟从创建到完成」不同，**只累计 `executing` 段**；待就位、暂停、插单等待不计入。

| 字段/函数 | 作用 |
|-----------|------|
| `effectiveWorkSeconds` | 已落库的累计秒数 |
| `workSegmentStartedAt` | 当前执行片段开始时间；`start` / `setStatus→executing` 时设为 `now` |
| `activeSegmentStart(task)` | `executing` 时返回 `workSegmentStartedAt ?? startedAt` |
| `runningSegmentSeconds` | 当前片段未写入 DB 的已过秒数 |
| `totalEffectiveWorkSeconds` | 已结算 + 当前片段 |
| `flushExecutingSegment` | 在 **pause / complete / setStatus 离开 executing** 时：把片段时长加到 `effectiveWorkSeconds`，置 `workSegmentStartedAt=null` |
| `totalEffectiveWorkSecondsFromApi` | 前端/JSON 用；兼容旧数据：`paused` 且无累计时用 `startedAt→pausedAt` 墙钟差近似；`completed` 类似用 `completedAt` |
| `effectiveWorkMinutesFromApi` | 上式转分钟（展示用） |
| `taskSlotCapMinutes` | 时段上限：`category.maxDuration>0` 用 max，否则 `estDuration` |
| `overtimeMinutesBeyondSlot` | executing/paused 时有效工时分钟数超过上限则返回超出分钟数，否则 null |

**与 API 的衔接**：`PATCH … action=start` 设置 `workSegmentStartedAt=now`；`pause` 与 `setStatus` 中离开 executing 的路径调用 `flushExecutingSegment`；`complete` 经 `completeTask` 内部 `flushExecutingSegment`。

---

## 4. 任务生命周期

```
┌──────────────────────────────────────────────────────────────────┐
│                        任务生命周期                                │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  摄影师创建任务                                                    │
│       │                                                          │
│       ▼                                                          │
│  ┌─────────┐  有可派发助理 ┌──────────┐  助理确认   ┌──────────┐ │
│  │ waiting  │────────────→│ waiting   │──────────→│executing │ │
│  │(未分配)  │             │(已分配)    │  开始      │(执行中)   │ │
│  └─────────┘             └──────────┘            └──────────┘ │
│       │                                               │        │
│       │ 无可派发助理                        P1插单 │  正常完成  │
│       │ 留在队列                                ▼     │        │
│       │ 等待 sweep                       ┌──────────┐  │        │
│       │                                  │ paused   │  │        │
│       │                                  │(已暂停)   │  │        │
│       │                                  └──────────┘  │        │
│       │                                       │        │        │
│       │                                  P1完成后恢复   │        │
│       │                                       │        ▼        │
│       │                                       └→┌──────────┐   │
│       │                                         │completed │   │
│       │                                         │(已完成)   │   │
│       │                                         └──────────┘   │
│       │                                               │        │
│       │                                          释放助理       │
│       │                                          sweep扫描     │
│       └──────────────────────────────────────────────┘         │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**待就位让行（补充）**：当全楼无可派发助理时，更紧急的新单可通过 `interruptWaitingPreempt` 挂到「已待就位、尚未 executing」的助理名下：新单 `parentTaskId` 指向原较低优先 `waiting`，原单仍为 `waiting` 且仍占用该助理。子单完成后 `completeTask` 将父单恢复为 `waiting`（助理保持 `assigned`）。工作台地图与 Dock 对父单房间展示 **灰半透明头像** 等 UI，见 **6.2 互动地图** 小节。

### 任务状态说明

| 状态 | 条件 | 显示 |
|------|------|------|
| waiting + assistantId=null | 队列中，等待系统派单或进入公共队列 | "队列中" |
| waiting + assistantId 有值 | 已分配，等待助理就位 | "待就位" |
| executing | 助理正在执行 | "进行中" + 进度条 |
| paused | 助理暂停当前执行（含执行中插单流程中手动暂停等） | "已暂停" |
| completed | 完成 | "已完成" + 用时 |

### 优先级定义

优先级 P1-P6 的**具体任务名称和时长**由管理后台「任务逻辑 → 任务优先级设定」中的任务类型配置与专项快捷入口决定，代码不硬编码普通任务内容；「外模跟拍协助」专项快捷入口固定为 P6。

| 等级 | 说明 | 可被插单 |
|------|------|----------|
| P1 | 最高优先级 | 不会被插，且可插断 P2-P6 |
| P2 | 高优先级 | 可被 P1 插单（除非锁定） |
| P3 | 中优先级 | 可被 P1 插单（除非锁定） |
| P4 | 低优先级 | 可被 P1 插单（除非锁定） |
| P5 | 最低优先级 | 可被 P1 插单（除非锁定） |
| P6 | 专项最低优先级，用于外模跟拍协助 | 执行中不可被插单；未开始前仍可按待就位让行规则重排 |

管理员在后台可以为每个优先级添加多种任务类型，并设定各类型的时长范围。摄影师创建任务时选择任务类型，系统自动继承该类型的优先级。

---

## 5. API 接口清单

### 5.0 全量路由索引

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 工号+密码登录，返回 profile（不含 password） |
| GET | `/api/tasks` | 任务列表；纯读取，不触发维护和派单 |
| POST | `/api/tasks` | 创建任务 |
| DELETE | `/api/tasks` | 按摄影师批量删未完成任务 |
| GET | `/api/tasks/[id]` | 单任务详情（含 parent / interruptTasks） |
| PATCH | `/api/tasks/[id]` | 状态机操作，见 **§5.1.1** 表 |
| DELETE | `/api/tasks/[id]` | 删单；有助理则置 idle；`sweepWaitingTasks()` |
| POST | `/api/tasks/sweep` | 手动触发 `sweepWaitingTasks()`，返回 `{ assigned }` |
| GET | `/api/profiles` | 用户列表 |
| POST | `/api/profiles` | 创建用户 |
| GET | `/api/profiles/[id]` | 用户详情 + 最近未完成创建/分配任务 |
| PATCH | `/api/profiles/[id]` | 更新用户；见下 |
| DELETE | `/api/profiles/[id]` | 删除用户 |
| POST | `/api/profiles/batch` | Excel 批量导入 |
| GET | `/api/buildings` | 楼座与房间 |
| POST | `/api/buildings` | 创建楼座 |
| PATCH | `/api/buildings/[id]` | 更新楼座（含图片） |
| DELETE | `/api/buildings/[id]` | 删除楼座 |
| POST/PATCH/DELETE | `/api/buildings/[id]/rooms` | 房间增改删 |
| GET/POST | `/api/categories` | 任务类型 |
| PATCH/DELETE | `/api/categories/[id]` | 单类型 |
| PUT | `/api/categories/reorder` | 排序 |
| GET/PUT | `/api/config` | 系统配置键值 |

### 5.1 任务接口

#### `GET /api/tasks` — 查询任务列表

| 参数 | 类型 | 说明 |
|------|------|------|
| status | string | 按状态过滤 |
| priority | number | 按优先级过滤 |
| photographerId | string | 按摄影师过滤 |
| assistantId | string | 按助理过滤 |
| todayOnly | "true" | 只返回今天的任务 |
| weekOnly | "true" | 只返回本周的任务 |
| buildingId | number | 按任务发生楼座过滤；优先匹配 `locationBuildingId`，旧数据回退摄影师登记楼座 |
| limit | number | 限制返回条数；服务端最大 500，避免高并发下全量拉取 |
| view | photographer \| assistant \| admin \| building | 角色视图；`photographer/assistant` 可配合 `profileId` 自动映射为对应身份过滤 |
| profileId | string | `view=photographer/assistant` 时使用的身份 ID |
| includeCollaborators | "false" | 显式传 `false` 时不返回 collaborators 明细，用于统计/轻量列表 |

- 当前接口是纯读取：不执行 `cleanupStaleTasks()`、`syncProfileStatus()`、`escalatePriorities()` 或 `sweepWaitingTasks()`，避免高频轮询放大全局写入压力。
- 需要维护或派单时，由写入型接口、状态变化接口或 `POST /api/tasks/sweep` 触发 `runTaskMaintenance()`。
- 查询边界口径：
  - 摄影师个人任务：`view=photographer&profileId=...&todayOnly=true&limit=...`。
  - 助理个人任务：`view=assistant&profileId=...&todayOnly=true`；仍会包含该助理作为协作参与者的任务。
  - 当前区域公共/地图数据：`buildingId=...&todayOnly=true&limit=...`；需要协作头像时保留默认 collaborators，纯统计时传 `includeCollaborators=false`。
  - 后台统计：按日期范围或周视图拉取时，应优先带 `limit`、`buildingId` 或身份过滤；全量查询只用于管理员主动打开统计页，不应作为高频轮询。
- 排序：优先级升序 → 创建时间升序

#### 工作台请求边界建议

| 角色/入口 | 需要的数据 | 建议请求 |
|-----------|------------|----------|
| 摄影师桌面/手机“我的任务” | 本人今日任务、指定标识、主助理、协作概览 | `GET /api/tasks?view=photographer&profileId=...&todayOnly=true&limit=100` |
| 助理桌面/手机当前任务 | 本人主任务 + 协作任务、参与记录、转派通知 | `GET /api/tasks?view=assistant&profileId=...&todayOnly=true&limit=100` + `/api/reassignment-notices?assistantId=...` |
| 当前楼座地图/公共队列 | 当前楼座今日任务、参与者头像、父子任务必要补拉 | `GET /api/tasks?buildingId=...&todayOnly=true&limit=300`，缺失父任务再按 ID 补拉 |
| 后台审批 | 待就位指定任务，后续可轻量化 | `GET /api/tasks?status=waiting&includeCollaborators=false&limit=200` |
| 后台统计 | 周/日范围任务、人员和楼座字典 | `GET /api/tasks?weekOnly=true&weekOffset=...&includeCollaborators=false&limit=500`，再按筛选条件收窄 |

#### API 权限审计（只读记录，待后续登录态改造）

- 当前多处接口仍信任前端传入的 `profileId / assistantId / photographerId / actorAssistantId`，缺少统一服务端登录态校验。
- 高风险写接口：
  - `POST /api/tasks`：信任 `photographerId` 创建任务；后续应校验登录用户是否本人或管理员。
  - `PATCH /api/tasks/[id]`：信任 `actorAssistantId` 执行开始/暂停/完成；后续应校验任务参与者本人或管理员。
  - `DELETE /api/tasks/[id]` 与 `DELETE /api/tasks`：当前无登录态；后续应限制为发布摄影师本人、管理员或明确授权角色。
  - `POST /api/tasks/[id]/collaborators`：仅在传 `actorAssistantId` 时校验主助理，未传时偏管理入口；后续应区分摄影师、主助理和管理员权限。
  - `PATCH /api/profiles/[id]`、`POST /api/profiles`、`DELETE /api/profiles/[id]`、`PUT /api/config`、楼座/任务类型写接口：后续应限制管理员或具备管理权限账号。
  - `POST /api/tasks/sweep`、`PATCH /api/reassignment-notices/[id]`：后续应校验触发者身份，避免任意客户端反复触发或代确认。
- 只读接口如 `GET /api/tasks`、`GET /api/profiles`、`GET /api/config` 当前也无登录态；百人环境下应先通过查询边界降载，再在统一认证方案中补角色级可见范围。
- 初始密码 `123456` 本轮不改；风险保留为后续认证安全专项，不在本次 API 查询边界任务中实施。

#### 任务接口最小回归测试清单

- `/api/tasks` 查询参数：验证 `limit` 上限、非法参数 400、`buildingId` 过滤、`view=assistant` 返回协作任务、`includeCollaborators=false` 不返回 collaborators 明细。
- 指定助理：在线同楼座助理可指定；跨楼座、离线、非助理角色返回明确 4xx；忙碌助理可创建指定等待任务且不覆盖执行状态。
- 摄影师个人队列：达到发布上限后常规/指定发单进入 `photographer_active_task_limit_queue`，不提前占用指定助理。
- 取消边界：纯 `waiting` 可删；`executing/paused`、插单父子链路返回 `409 TASK_ALREADY_STARTED_CANNOT_CANCEL`。
- 熨烫等待保护：同楼座存在 `waiting_machine/notified` 压力时，执行中熨烫任务不可被 P1 执行中插单；无压力时保持原插单规则。

#### `POST /api/tasks` — 创建任务

| 参数 | 必填 | 说明 |
|------|------|------|
| photographerId | 是 | 摄影师 ID |
| roomNumber | 是 | 房间号 |
| categoryId | 是 | 任务类型 ID |
| priority | 否 | 优先级（默认用类型的 priorityLevel） |
| assistantId | 否 | 指定助理 ID |
| isSpecified | 否 | 是否指定助理模式 |
| isLocked | 否 | 是否锁定 |
| lockReason | isLocked=true 时必填 | 锁定原因 |
| estMinutes | 否 | 预计时长（分钟） |
| note | 否 | 备注 |

创建规则：

- 创建任务前先强制执行 `runTaskMaintenance({ force: true })`，确保昨天及更早的未完成任务先被每日清理，再计算摄影师发布上限。
- 初始动态优先级以任务类型配置 `priorityLevel` 为准，不信任前端传入的独立 `priority`。
- 任务发生楼座写入 `locationBuildingId`；派发、等待任务扫描、待就位让行、多人协作候选、公共队列和统计优先使用任务发生楼座。
- 非个人队列的指定助理模式使用现有 `isSpecified=true` + `assistantId`。后端必须先校验指定助理存在、角色为助理/助理组长、在线、无子状态、当前服务楼座匹配任务发生楼座；允许指定助理当前已有待执行、进行中、暂停或协作任务，不能因“忙碌/不可派发”直接拒绝创建。
- 指定助理任务校验通过后，创建时写入 `isSpecified=true`、`assistantId`，并维护 primary `TaskCollaborator`；仅当助理当前为 `idle` 时才把 `profile.status` 置为 `assigned`，避免覆盖正在执行/忙碌状态。非指定任务仍走原自动派单、待就位让行和执行中插单流程。
- 若摄影师已达到发布上限，指定助理请求和常规发单一样进入摄影师个人队列：任务标记 `isLocked=true`、`lockReason=photographer_active_task_limit_queue`、`assistantId=null`，暂不校验/占用指定助理，也不因指定助理直接 409。
- 创建任务返回前调用 `runTaskMaintenance()`，统一处理释放摄影师个人队列、状态同步、提权和后续派发。

创建后自动执行派单逻辑（非指定助理，`src/app/api/tasks/route.ts` 顺序）：

1. `assignTask()`：同楼座可派发助理直接分配（见 §2.5；仅有未开始熨烫等待的助理仍可接普通任务）；
2. `interruptWaitingPreempt()`：**待就位插单**（无空闲时）；
3. `interruptExecutingPreempt()`：筛选同楼座 **executing** 且可被插断、离场 cap 满足的新单，按 `buildP1InterruptCandidateOrderFromFiltered` 顺序尝试 `canInterrupt` + `interruptAssistant()`；
4. 仍无法分配则任务保持 **未分配 waiting**，依赖后续 `sweepWaitingTasks()` 或其它事件再次匹配。

#### `DELETE /api/tasks` — 批量删除任务

| 参数 | 说明 |
|------|------|
| photographerId | 按摄影师清空 |

- 只删除纯未开始任务：`status=waiting`、`parentTaskId=null`、且没有未完成的子插单任务。
- 释放被分配的助理 → 触发 `runTaskMaintenance({ force: true })`

#### `GET /api/tasks/[id]` — 单任务详情

- `include`：`photographer`、`assistant`、`category`、`parentTask`、`interruptTasks`（子插单列表）。

#### 5.1.1 `PATCH /api/tasks/[id]` 细则

请求体：`{ action, ... }`。非法 `action` 返回 400。

| action | 额外字段 | 行为摘要 |
|--------|----------|----------|
| **start** | `estMinutes?` | 事务内：`status=executing`；`startedAt` = 已有合法值或 `now`；`workSegmentStartedAt=now`；`estEndTime` = 由 `estMinutes` 推算或保留原值；`pausedAt=null`。若有 `assistantId`，对应 `profile.status=executing`。助理行必须存在，否则事务失败 500（dev 可带 details）。 |
| **pause** | — | 先 `flushExecutingSegment`；任务 `status=paused`、`pausedAt=now`、`workSegmentStartedAt=null`、写回累计秒数。若同助理存在 `waiting && parentTaskId!=null`（插单待就位），则 `profile.status=assigned`，否则 `busy`。 |
| **complete** | — | 调用 `completeTask(id)`（内含父任务/暂停恢复/idle+sweep 逻辑，见 §3.4.4）。 |
| **extend** | `estMinutes` 必填 | `estEndTime = now + estMinutes` |
| **setStatus** | `newStatus`：`waiting` \| `executing` \| `paused` \| `completed` | `completed` 等价于走 `completeTask`。其余：按目标状态合并 `startedAt` / `workSegmentStartedAt` / `flushExecutingSegment` / 清 `startedAt` 等；事务内同步 `profile.status`（executing→executing，paused→busy，waiting→assigned，其它→idle）。若新状态为 `waiting` 且**无** `assistantId`，额外调用 `assignTask(id)` 尝试自动分配。 |
| **transferPrimaryAssistant** | `actorAssistantId`, `targetAssistantId` | 助理端交换/移交 v1。当前主助理可把已开始且未完成的普通任务向同区域其他助理发起待确认请求；目标空闲时为 `handoff`，目标执行/暂停中时为 `swap`，发出请求不释放原助理。 |
| **respondPrimaryAssistantTransfer** | `actorAssistantId`, `accepted`, `responseMode?` | 目标助理处理待确认请求。空闲移交确认后进入 `ready_to_takeover`，目标助理点击开始接替时才真正切换负责人并释放原助理；忙碌互换必须传 `responseMode=pause_and_go` 或 `after_complete`。拒绝后请求终止。 |

**日期安全**：`coerceValidDate` 避免非法日期写库导致 Prisma 报错。

#### `DELETE /api/tasks/[id]` — 删除单个任务
- 只允许取消助理尚未点击开始的任务：`status=waiting` 且 `startedAt=null`。
- 等待熨烫机、准备熨烫、普通待就位、摄影师个人队列、未开始插单子任务、未开始插单父任务，只要本任务仍未点击开始，均可取消。
- 取消未开始插单父任务时，后端先把未完成子插单的 `parentTaskId` 置空，再删除父任务，避免断开数据库自关联。
- `paused/executing/completed`，以及曾经点击开始后又因暂停/插单让行显示为待就位的任务，返回 `409 TASK_ALREADY_STARTED_CANNOT_CANCEL`。
- 删除后同步助理状态并触发 `runTaskMaintenance({ force: true })`

#### `POST /api/tasks/sweep` — 手动触发全局派单扫描

- 调用 `runTaskMaintenance()`，返回 `{ cleaned, escalated, assigned, skipped }`。
- `assigned` 为 `sweepWaitingTasks()` 本次成功释放、转派、推进、均衡、直接分配、待就位让行和执行中插单的合计次数。

### 5.1a 认证

#### `POST /api/auth/login`

- Body：`{ employeeId, password }`。
- 成功：返回 profile JSON（**剔除** `password` 字段），含 `building`。
- 失败：400 缺参；401 工号不存在或密码错误。

### 5.2 用户接口

#### `GET /api/profiles` — 查询用户列表

| 参数 | 说明 |
|------|------|
| role | 按角色过滤（assistant 会同时包含 assistant_leader） |
| buildingId | 按楼座过滤 |
| status | 按状态过滤 |

#### `POST /api/profiles` — 创建用户
- 必填：`name`, `role`, `buildingId`；`role` 须为合法枚举。
- 可选：`employeeId`, `currentRoom`, `avatar`, `department`, `group`。

#### `PATCH /api/profiles/[id]` — 更新用户
- Body 中出现的字段才会更新：`status`, `subStatus`, `currentRoom`, `buildingId`, `isOnline`, `name`, `role`, `avatar`, `employeeId`, `onlineStatus`, `department`, `group`。
- `status` 必须是合法 `ProfileStatus`。
- **业务规范**：助理 / 助理组长在存在真实占用工作时禁止修改 `onlineStatus`，否则 400（文案含当前状态中文标签）；仅有未开始熨烫等待不应被当作真实占用工作。
- **派单触发**：仅当角色为助理类，且从「非可派发」变为「可派发」时异步调用 `sweepWaitingTasks()`（可派发口径见 §2.5）。

#### `DELETE /api/profiles/[id]` — 删除用户

#### `POST /api/profiles/batch` — 批量导入
- 支持 Excel 导入，按 employeeId 做 upsert

### 5.3 楼座/房间接口

#### `GET /api/buildings` — 查询所有楼座（含房间）
#### `POST /api/buildings` — 创建楼座
#### `PATCH /api/buildings/[id]` — 更新楼座（支持图片上传）
#### `DELETE /api/buildings/[id]` — 删除楼座（级联删除房间）

#### `POST /api/buildings/[id]/rooms` — 添加房间
#### `PATCH /api/buildings/[id]/rooms` — 更新房间坐标
#### `DELETE /api/buildings/[id]/rooms?roomId=xxx` — 删除房间

### 5.4 任务类型接口

#### `GET /api/categories` — 查询所有任务类型
#### `POST /api/categories` — 创建任务类型
#### `PATCH /api/categories/[id]` — 更新任务类型
#### `DELETE /api/categories/[id]` — 删除任务类型（级联删除关联任务）
#### `PUT /api/categories/reorder` — 批量更新排序

### 5.5 系统配置接口

#### `GET /api/config` — 获取所有配置
- 返回 `Record<key, { value, label }>`。

#### `PUT /api/config` — 更新配置（upsert）
- Body：扁平 `{ key: value, ... }`，值会 `String(value)` 后写入；无则 create、有则 update。

---

## 6. 页面功能

### 6.1 首页 `/`

- 4 个角色入口卡片：摄影师 / 助理 / 组长 / 管理员
- 显示统计数据：楼座数、摄影师数、助理数

### 6.2 工作台面板 `/photographer`

> 摄影师与助理共用此页面，区别在于左侧工作区内容不同。

**右上角按钮权限**（按登录账号角色判断，非切换后的角色）

| 角色 | 返回登录 | 数据统计 | 切换身份 | 后台管理 |
|------|----------|----------|----------|----------|
| photographer | 可见 | 可见 | 不可见 | 不可见 |
| assistant | 可见 | 可见 | 不可见 | 不可见 |
| assistant_leader | 可见 | 不可见 | 可见 | 可见 |
| admin | 可见 | 不可见 | 可见 | 可见 |

- 「数据统计」跳转 `/stats`，复用管理后台 StatsTab 组件

- 「返回登录」按钮清除 localStorage 登录信息后跳转首页

**核心功能面板（2400+ 行）**

#### 楼座切换（Dock 风格标签栏）
- 可拖拽排序的楼座标签
- 显示每个楼座的助理人数
- 切换后加载对应楼座的地图和助理

#### 任务列表
- 按状态分组显示：待就位 → 队列中 → 进行中 → 已完成 → 已暂停
- 执行中任务显示进度条（基于 startedAt + estEndTime）
- 时间显示："已执行 X分钟/Y分钟"、"用时 X分钟"
- 任务创建入口跳转到 `/photographer/book`

#### 互动地图

> 核心实现：`src/app/photographer/page.tsx`（助理标记、任务聚合与地图层叠逻辑）。

- 加载楼座的楼层平面图
- 鼠标滚轮缩放（1-5x）、拖拽平移
- 触摸屏双指缩放和平移
- 房间标记点（圆形 + 围栏圆圈可视化）
- 额外场地标记（旋转方块）
- 裁剪区域自动居中显示
- 拖拽房间可更新坐标

**同一助理多状态标记**（有坐标即画）：主头像（`currentRoom`）、待就位让行 **灰 50% 头像**（`preemptedWaitingRoom`，父任务仍为 waiting）、**暂停**灰头像（`pausedRoom`）、**紧急插单待处理**蓝脉冲（`pendingRoom`，执行中且另有待就位插单待处理场景）。可同时存在，需与右侧 Dock 状态一致。

**跨助理同房间错开**：同一房间号上可能存在多名助理的不同标记（例如 A 的执行中主标记与 B 的父任务灰头像同坐标）。地图侧按房间聚合 **四类 slot**（`main` / `preempt` / `paused` / `pending`，键含 `assistantId`），房间内排序后横向偏移 `(i - (n-1)/2) * 22px`，避免完全重叠导致灰头像被主标记盖住。悬停时 z-index 提高。

**灰头像悬停文案**：第一行 `{助理姓名} · {preemptedWaitingTaskDesc}`；`preemptedWaitingTaskDesc` 仅为 **`已让行紧急单`** 或 **`已超时…`**（不含任务类型与「等待继续」）。第二行 `preemptedWaitingTaskDetail` 为房间 · 类型 · 时段说明（`buildDesc`）。

**数据侧注意**：列表接口常带 `todayOnly`，父任务可能不在当日列表中；`refreshAssistants` 会对缺失的 `parentTaskId` **按需 `GET /api/tasks/{id}`** 补全父任务，否则无法解析 `preemptedWaitingRoom`。聚合规则需识别「执行中子单的 `parentTaskId` 指向待就位父单」等组合（与 `resolveAssistantTasks` / 助理面板一致）。

#### 助理面板（右侧 Dock）
- 固定右侧，鼠标悬停时展开
- 余弦插值动画
- 状态点颜色见 **[2.2.1 工作台助理状态点](#221-工作台助理状态点右侧-dock身份切换列表)**（绿空闲中 / 蓝待就位 / 橙进行中与在忙 / 红已超时 / 灰已下线；已取消浅绿「快结束」点）
- 离线助理灰显
- 悬停提示：姓名、状态、当前房间、当前任务、已执行时间

#### 身份管理弹窗
- 显示当前用户信息
- 所有人员列表，支持角色/楼座/部门/组别筛选
- 切换助理上下线状态、所属楼座

#### 统计弹窗
- 本周任务统计
- 按天分组柱状图
- 按任务类型统计

#### 自动同步
- 8 秒轮询刷新任务和助理状态

### 6.3 预约页面 `/photographer/book`

- 任务创建表单：
  - 任务类型选择器（P1-P4）
  - 房间号输入
  - 预计时长
  - 开关：指定助理、锁定任务
  - 备注字段

### 6.4 助理页面 `/assistant`

- 静态 UI 原型（暂未完全实现）
- 显示：自动接单开关、状态按钮、今日统计、当前任务、等待队列、完成记录

### 6.5 组长页面 `/leader`

- 仪表盘布局，左侧导航
- KPI 统计：等待任务数、执行中任务数、空闲助理数、今日完成数
- 审批队列
- 助理状态网格（按楼座）
- 任务队列表（优先级、类型、房间、摄影师、助理、状态）

### 6.6 管理后台 `/admin`

Chrome 风格标签页界面，5 个 Tab：

#### Tab 1：人员管理
- 统计卡片：摄影师/助理/管理员/总人数
- 筛选：角色、楼座、部门、组别、搜索
- 表格：姓名、工号、角色、部门、组别、楼座、房间、在线状态
- 操作：编辑、删除、切换上下线
- Excel 批量导入（模板下载 + 拖拽上传 + 预览 + 导入结果）

#### Tab 2：空间管理
- 楼座列表（左侧）+ 楼座详情（右侧）
- 楼层平面图上传（拖拽上传）
- MapEditor 地图编辑器：
  - 房间标记（可拖拽、显示房间号、围栏半径圈）
  - 额外场地标记（旋转方块、可拖拽）
  - 裁剪区域选择（角拖拽调整大小）
- 房间列表表格

#### Tab 3：任务逻辑
- 任务类型管理：
  - 按优先级或名称分组查看
  - 编辑：名称、优先级、时长范围
  - 删除（级联删除关联任务）
  - 新增类型
- 全局参数配置（见第 7 节）

#### Tab 4：审批管理
- 静态 UI 原型

#### Tab 5：数据统计
- 静态 UI 原型

### 6.7 贪吃蛇游戏 `/snake`

- 独立的贪吃蛇小游戏页面

---

## 7. 系统配置参数

| 配置键 | 名称 | 范围 | 默认值 | 说明 |
|--------|------|------|--------|------|
| upgrade_threshold | 提权等待时长 | 10-60 分钟 | 30 | 任务等待超过此时间自动升级优先级 |
| ending_alert_min | 结项预警时间 | 1-10 分钟 | 2 | 距预计结束时间多少分钟开始预警 |
| eating_overtime_alert_min | 吃饭累计时长上限/提醒 | 业务配置 | 30 | 助理本次吃饭累计超过此时间后提醒，并禁止继续进入吃饭 |
| eating_reentry_cooldown_min | 结束吃饭后再次切换冷却 | 业务配置 | 30 | 助理点击结束吃饭后，短时间内不能再次切回吃饭中 |
| interruption_max | 最长中断时间 | 10-60 分钟 | 30 | 执行中插单时，新任务「离场」上界与当前任务可接受离场 cap 比较；与类型 `maxInterruptMinutes` 取较小值 |
| ironing_prep_window_min | 熨烫机准备窗口 | 1-30 分钟 | 5 | 机器即将/已经可用时，用于提醒下一位助理准备就位 |
| ironing_confirm_timeout_sec | 熨烫机空出确认倒计时 | 15-300 秒 | 60 | 熨烫机空出后，助理确认前往或开始前的提示倒计时 |
| ironing_machine_claim_ttl_min | 熨烫机使用权保护时间 | 1-30 分钟 | 2 | `notified` 准备熨烫的机器使用权保留时长；超时未开始则释放回队列 |
| standby_reassign_timeout_min | 待就位超时多久更换派发助理 | 1-120 分钟 | 20 | 普通待就位和 `notified` 准备熨烫超时后，可转派同楼座可派发助理 |
| photographer_max_active_tasks | 摄影师最多同时下达任务数 | 1/2/3/4 | 1 | 摄影师未完成任务达到上限时，新任务进入个人限流队列 |
| collaboration_queue_auto_close_limit | 自动关闭多人协作的队列阈值 | 业务配置 | 10 | 当前区域任务队列达到阈值时，可自动关闭多人协作 |
| p1_interrupt_dispatch_mode | P1/紧急插单候选排序 | 见说明 | `priority_tier_rr` | `priority_tier_rr`：按当前执行 priority 分档，档内轮询；`flat_round_robin`：全体候选轮询。管理后台「任务逻辑」Tab 可改 |
| workbench_page_background | 工作台页面背景图 | URL/空 | 空 | 工作台页面背景图配置 |

**说明**：每楼座轮询指针 `p1_interrupt_rr_b{buildingId}` 由调度器读写，一般不在后台表单中直接编辑。

---

## 8. 实时通信

> 服务端：`src/lib/socket.ts`；事件名字符串常量：`src/types/index.ts` 中的 `SOCKET_EVENTS`（与 emit 一致）。

### Socket.io 事件

| 事件 | 方向 | 说明 |
|------|------|------|
| `join:role` | 客户端→服务端 | 加入角色广播房间 |
| `join:building` | 客户端→服务端 | 加入楼座广播房间 |
| `join:user` | 客户端→服务端 | 加入个人房间 |
| `task:created` | 服务端→客户端 | 新任务创建通知 |
| `task:updated` | 服务端→客户端 | 任务状态更新通知 |
| `task:assigned` | 服务端→客户端 | 任务分配通知 |
| `profile:updated` | 服务端→客户端 | 用户状态变更通知 |
| `alert:urgent` | 服务端→客户端 | P1 紧急任务警报（定向推送） |
| `alert:completion` | 服务端→客户端 | 任务即将完成预警 |

---

## 9. 业务规则分类沉淀

> 新增或修改业务逻辑时，优先添加到本节对应大类别的小节中；不要再按日期把新规则堆成流水账。日期记录只保留为历史归档索引，当前有效口径以本节和前文模型/API/调度章节为准。

### 9.1 派发、公共队列与提权

#### 普通派发与公共队列

- 新任务自动派发优先在任务发生楼座/当前大区内选择可派发助理，不跨区域随意取人；任务发生楼座优先取 `BookingTask.locationBuildingId`，缺失时才回退摄影师登记楼座。
- 可派发候选以 §2.5 为准：在线、无微标签、同楼座，且没有真实占用工作；仅有未开始熨烫等待仍可接普通任务。
- 系统派发普通任务时，优先找同楼座真正空闲助理；如果某助理只挂着未开始熨烫等待，也可以作为可派发候选。
- 同一区域可派发助理较多时，派发应按楼座维度轮换，避免短时间内反复派给同一位助理。
- 没有可派发助理时，未分配 `waiting` 任务进入公共队列；摄影师“我的任务”中这类任务显示为“队列中”，并展示已等待时长。
- 公共队列不取决于摄影师发布的是第几条任务；只要任务未分配助理且仍在等待，就属于公共队列。
- 已分配助理但尚未开始的 `waiting` 任务显示为“待就位”，并体现为已有处理进展，不能继续显示含糊的“等待中”。
- 公共队列展示优先按处理进度排序：已分配待就位优先，其次未分配任务，暂停任务靠后；同一阶段内再按 `P级 -> 创建时间/等待时间` 排序。
- 公共队列中的排名、提权标记、待就位超时标记、本人任务高亮和插入动画只用于解释队列状态，不改变真实派发规则。
- 公共队列中的人员/头像/任务展示顺序应与实际队列顺序一致，不能按单量多少重新排序。
- 公共队列和“我的任务”中，未完成提权任务优先显示 `P初始→P当前`；旧数据没有 `escalatedFromPriority` 时回退显示“提权”。
- 已完成任务即使曾经提权，也不再展示提权标识，避免把历史过程当成当前待处理风险。

#### 等待提权

- 动态提权覆盖所有尚未开始的 `waiting` 任务，包括未分配任务和已分配但尚未就位的任务。
- 已分配待就位任务的等待起点优先使用主助理参与记录 `joinedAt`；缺失时回退任务 `createdAt`。
- 摄影师发布上限锁住的个人队列任务不参与普通提权，必须等发布名额释放后再进入正常公共队列/派发流程。
- 系统主动扫描待分配任务时，行为应与摄影师新建任务后的派发行为一致：先匹配同楼座可派发助理，没有候选时再进入待就位让行/公共队列等后续规则。

#### 摄影师发布上限与个人队列

- 后台可配置摄影师最大未完成任务数；未完成任务统计口径包含 `waiting / executing / paused`，但不包含摄影师发布上限锁住的个人队列任务本身。
- 新建任务接口必须先执行过期任务清理，再判断摄影师是否达到发布上限；跨天遗留任务应先自动完成，不得造成“当前发布任务为空但无法发单”。
- 达到发布上限后，不直接拒绝摄影师创建任务，而是创建摄影师个人队列任务。
- 个人队列任务使用 `isLocked + lockReason=photographer_active_task_limit_queue` 标记，不新增额外业务字段。
- 同一摄影师最多只保留 1 条 `photographer_active_task_limit_queue` 个人队列任务；已有 1 条后继续发布返回 `409 / PHOTOGRAPHER_LIMIT_QUEUE_FULL`，不再创建新任务。
- 个人队列任务不进入公共队列、自动派发、空闲认领、全局扫描和动态提权。
- 当摄影师未完成任务数低于上限时，系统自动释放最早的个人队列任务进入正常派发队列。
- 个人队列任务释放时，释放顺序仍按个人队列内原始创建时间最早优先；被释放任务进入公共队列时刷新 `createdAt` 为释放时间，并清空 `escalatedAt`，公共排队等待、排序和提权从释放时间重新计算。
- 摄影师“我的任务”中个人队列任务排在未完成任务顶部，明确提示“达到最大任务发布量，待上一任务完结后进入公共队列排队”。
- 摄影师端对个人队列任务显示为灰色“队列中”，并提示需要等上一任务完结后再进入公共队列排队。

### 9.2 插单、让行与待就位转派

#### 插单路径边界

- “待就位让行”和“执行中插单”是两条不同逻辑。
- 待就位让行：原任务尚未实际开始执行，更高优先级任务可以让较低优先级待就位任务让行；此时执行中插单的最大离场时间不作为硬限制。
- 待就位让行仍要求新任务比当前待就位任务更紧急，即新任务 priority 数值更小；当低优先级待就位任务提权到同级或更高后，同级任务不能继续抢它。
- 执行中插单：原任务已经开始执行时，必须读取任务类型配置，包括是否可插单、最大离场时间和全局 `interruption_max`。
- 执行中插单还必须满足当前任务未锁定、执行中任务不是最高优先级任务、新任务优先级更高、且新任务离场时长不超过当前任务允许离场上限。
- 最大离场时间只参与执行中插单判断，不应误用于待就位让行。
- 待就位让行基本安全条件：新任务更紧急、原任务尚未开始、没有重复让行子单、助理没有其他活跃插单。
- 同一助理已有未完成插单任务时，不再继续叠加新的临时接派任务。
- 待就位让行只允许该助理当前只有一个活跃待就位父任务，且没有任何活跃插单；不能在不同父任务路径上连续叠加多条临时接派任务。
- 待就位让行也要校验原任务允许离场上限：新任务最长时长必须小于等于原任务可让行最大时间；例如 10-30 分钟新单按 30 分钟判断。
- 执行中插单、待就位让行和候选筛选均按当前服务楼座/任务发生楼座判断；助理切换服务区域后，不能继续按登记楼座参与旧区域派发。

#### 父子任务恢复

- 发生插单后，需要区分原主任务和紧急子任务；若紧急子任务暂停，点击继续应恢复紧急子任务本身，而不是直接恢复原主任务。
- 子任务完成后，父任务恢复为 `waiting`，助理保持 `assigned`，等待重新就位继续。

#### 待就位超时换人

- 待就位任务超时后，系统可以把该待就位任务转派给新的可派发助理。
- 待就位超时扫描由 `sweepWaitingTasks()` 统一执行，工作台轮询需定期触发维护扫描；不能只依赖发单、改状态或切换地点这类偶发事件。
- 待就位超时换人只在同楼座/同服务区域内寻找新的可派发助理。
- 换人优先级可按任务紧急程度和超时等待时长综合排序：任务越急、等待越久，越优先接替。
- 换人事务中：任务主助理切换为新助理；旧 primary 参与记录标记为 `left`；新助理写入 `primary / waiting`；任务备注保持不变。
- 若当前负责人超时且同楼座暂时没有可派发助理：普通待就位任务释放为 `assistantId=null` 的公共队列任务，当前负责人置为 `offline + idle`；后续有新助理可派发时由 `sweepWaitingTasks()` 继续分配。
- 准备熨烫 `notified` 超时但无替换人时不释放为普通公共队列，保留任务归属/机器准备口径，只把当前负责人置离线，避免熨烫机使用权和普通任务队列混淆。
- 待就位等待时长展示优先使用当前 primary 参与记录的 `joinedAt`；未分配队列才使用任务 `createdAt`。转派后不能继续把旧负责人等待时间算给新负责人。
- 任务转派必须先收敛旧参与记录：尤其是 `role=primary` 的任务只能保留一个当前主负责人；把新助理 upsert 为 primary 前，要把旧 primary 标记为 `left`。
- 待就位超时换人必须与自动离线解耦：如果旧助理仍有其他 `executing / paused` 主任务或协作任务，只转派这条待就位任务，不把旧助理下线。
- 只有旧助理没有其他进行中/暂停中工作时，待就位超时换人才可以把旧助理切为 `offline + idle`；旧助理确认通知后再恢复在线。
- 转派通知需要记录原因、旧助理是否被下线、旧助理当时仍在处理的任务房间/类型/状态，便于前端解释。

#### 双任务未选待就位任务释放

- 当助理身上同时有多条任务，并且她已经选择其中一条进入真实 `executing / paused` 状态后，其余未选择的普通已分配待就位任务可以释放给同楼座空闲助理。
- 只处理普通待就位任务：`status=waiting`、`assistantId != null`、`ironingStage=none`、`parentTaskId=null`。
- 原负责人必须已经有真实执行/暂停主任务或协作参与记录；如果原负责人只是单任务待就位，不自动抢走她的任务。
- 不处理等待熨烫机、准备熨烫、熨烫队列任务；这些仍走熨烫机队列、机器使用权保护和熨烫等待均衡规则。
- 不处理摄影师限流锁定任务，也不处理插单父子任务，避免破坏限流队列和插单恢复链路。
- 不等待普通待就位超时阈值；只要同楼座出现可派发空闲助理，就可以转派这条未选普通待就位任务。
- 转派事务要求：主任务 `assistantId` 改为新助理；旧 primary 参与记录置为 `left`；新助理 primary 参与记录置为 `waiting`；新助理状态置为 `assigned`。
- 转派通知记录使用 `standby_reassignment_notices.reason=unselected_standby_release`，旧助理不因此自动下线。

### 9.3 熨烫机、熨烫等待与机器使用权

- 熨烫任务识别口径为任务类型归组为「熨烫」或任务类型名称包含「熨」。
- 后台「空间管理」管理熨烫机数量和状态；熨烫机归属具体楼座/区域，可新增、删除、设置正常运行或维修不可用。
- 熨烫机默认按数字序号命名，例如 `熨烫机1`、`熨烫机2`；前台不使用 A/B/C 命名口径。
- 熨烫机不作为可拖拽地图点位展示；前台地图右上角固定展示熨烫机接续区。
- 右上角接续区不使用外层底框/玻璃面板，常态只显示熨烫机图标与助理头像。
- 右上角熨烫接续区可以折叠：折叠后只显示熨烫区繁忙/适中/空闲概览与图标，展开后再显示每台机器的头像队列。
- 折叠态的繁忙度判断分四档：`空闲`=空机器大于正在使用中的机器，`适中`=还有空机器但不明显富余，`繁忙`=所有正常机器都在运行且无排队，`拥挤`=所有正常机器都在运行且有排队；其中 `繁忙 / 拥挤` 都用红色呈现。
- 折叠态只显示“熨烫机 + 状态”文本和右侧熨烫机图标，不使用底框、第二行统计或额外状态词，图标位置与展开态的机器图标对齐。
- 右上角每台机器一行：
  - 进行中机器显示红色熨斗图标；
  - 空闲正常机器显示绿色熨斗图标；
  - 维修不可用机器显示灰色熨斗图标；
  - 机器行按 `进行中 -> 空闲 -> 维修不可用` 排序，同状态内按后台排序和机器 id 稳定排序。
- 熨斗图标 hover 只表达机器状态，不再显示 A/B/C 这类旧机器名称。
- 每台正常运行熨烫机同一时间最多绑定 1 个正在使用助理头像；维修机器不绑定助理。
- 开始熨烫前，后端必须校验同楼座正常运行机器容量；机器已满时返回明确错误，不允许进入执行中。
- 维修不可用机器不参与可用容量计算。
- 熨烫任务阶段分为 `none / waiting_machine / notified / using`。
- `waiting_machine`、`notified`、以及历史口径里的 `waiting + 熨烫 + 已分配 + stage=none` 都视为未开始熨烫等待，不等于助理正在工作。
- `notified` 表示机器已空出或即将可用，是临时机器使用权；只有进入 `using/executing` 后才实际占用熨烫机。
- 当同楼座已经有 `waiting_machine/notified` 熨烫等待压力时，正在执行熨烫任务的助理受机器队列保护，不允许被执行中插单离场；待就位让行、熨烫等待均衡、熨烫机队列推进和空闲助理派发不受这条保护影响。
- 状态同步、派发候选、待就位让行和自动认领都应排除被动等待熨烫机的任务，避免把多个历史负责人误同步成待就位，或允许多人误开始同一主任务。
- 熨烫机空档处理顺序：
  - 先处理普通待就位/准备熨烫待就位超时换人；
  - 再释放超过保护期仍未开始的 `notified` 机器使用权；
  - 再扫描熨烫机队列，把可开始的熨烫任务推进到 `notified`；
  - 再做熨烫等待负载均衡；
  - 最后再派发普通未分配任务。
- 机器使用权有保护时间，默认 2 分钟，可在后台逻辑设置调整；超时未开始会释放使用权并回到 `waiting_machine` 队列。
- 当机器空出后，队首熨烫任务进入 `notified`；超过保护期仍未开始时，释放机器使用权，任务回到 `waiting_machine` 并刷新排队时间，避免空机器被长期占住。
- 只有一台或多台机器全满时，等待头像按每台机器预计可用时间轴动态排队；排在前面的等待任务预计时长也计入该机器后续可用时间。
- 如果所有机器中有助理提前完成或超时释放，系统应重新计算最优机器队列，让后续等待任务排到预计最早可用的机器后面。
- 有空闲机器时，准备熨烫助理头像直接挂到空闲机器左侧，不继续排在忙碌机器后面。
- 等待/准备熨烫头像显示在对应熨烫机图标左侧，队列按先后从靠近机器处向左延展，不推动机器图标横向移动。
- 等待/准备熨烫头像 hover 状态按机器行判断：前面有正在使用助理时显示“队列中”；机器已空出并获得使用权时显示“准备熨烫”。
- 熨烫任务头像气泡显示任务时长、已进行时长、预计剩余或已超时；超过 60 分钟的时长用小时显示。
- 同楼座有可派发助理时，未开始熨烫等待不能无限集中在同一人身上：保留已获得机器使用权的 `notified`，优先把额外 `waiting_machine/stage=none` 自动均衡给没有熨烫等待的助理。
- 熨烫等待自动均衡的转派记录原因使用 `ironing_wait_load_balance`。
- 熨烫等待自动均衡转派时，应把旧 primary 参与记录标记为 `left`，给新助理创建/更新 primary waiting，并记录转派原因。
- 等待熨烫期间，系统可以继续给负责人派发其他更合适或更高优先级的普通任务；这不取消她的熨烫等待身份。
- 普通待就位任务与熨烫等待的关系：
  - 如果负责人只是等待熨烫机，她仍可先处理其他普通待就位任务；
  - 普通任务只有在负责人真正去熨烫后，超过待就位超时阈值才可转派给其他空闲助理；
  - 不能因为她还在等待机器，就提前把她的普通待就位任务转走并让她自动离线。
- 如果助理同时有普通待就位和 `notified` 准备熨烫，当前任务状态应展示多个可点击入口；`waiting_machine` 只显示等待提示，不可点击开始。
- 熨烫任务进入助理“可开始任务池”必须满足机器条件：当前区域存在空闲熨烫机，或该任务已经进入 `notified` 准备熨烫；如果所有熨烫机都忙，熨烫任务只能显示“等待熨烫机/排队中”，不应作为可点击开始项压过普通待就位任务。
- 后端 `PATCH /api/tasks/[id] action=start` 必须兜底执行同一规则：当前助理临时接手公共队列或其他未开始负责人名下的熨烫任务时，只允许 `waiting` 熨烫任务，且助理在线、同楼座、没有执行/暂停真实工作；任务必须已 `notified` 或当前区域有空闲熨烫机，否则返回 409。
- 熨烫任务临时接手/改派必须收敛 primary：旧 primary 标记 `left`，当前助理写入 `primary/waiting` 后再进入 `executing/using`；普通任务不开放这种跨人抢开始能力。

### 9.4 助理状态、在线权限与楼座切换

#### 身份与在线状态

- 身份切换面板用于选择身份和查看状态，不应成为管理账号绕过业务限制的入口。
- 只有当前登录/切换到的助理本人可以切换自己的 `online / on_break / offline` 等在线状态；非当前助理状态只读。
- 在线、吃饭、休假/下班这类会影响派发候选池的状态变更，需要经过当前身份本人及任务状态校验；后端接口必须强制校验，不能只靠前端按钮隐藏。
- 助理只有在真实工作中才禁止直接切换吃饭/休假/下班：`executing/finishing` 或已经开始过再暂停的任务属于真实工作；系统已派发但尚未开始的 `waiting/待就位` 不属于真实工作。
- 助理从未开始的待就位状态切换为吃饭/休假/下班时，系统应释放该助理名下未开始任务，清理当前 primary 参与记录，并重新进入派发队列。
- 「休假/下班」统一表达为「休假/下班/离线」，前端展示、身份切换列表和状态点需保持一致。
- 后台在线状态菜单不要同时提供“离线”和“休假/下班/离线”两个离开入口；业务上保留一个离开状态入口，旧数据中的 `offline` 仅做展示兼容。

#### 吃饭状态

- 吃饭状态必须使用独立字段记录开始、暂停、结束和累计时长，例如 `eatingStartedAt`、`eatingPausedAt`、`eatingEndedAt`、`eatingAccumulatedSeconds`，不能用 `Profile.updatedAt` 推算。
- 暂停吃饭不等于结束吃饭：助理吃饭中遇到临时紧急任务时，可以暂停吃饭并回到在线状态，暂停期间不计入吃饭时长，后续可以继续吃饭。
- `eatingPausedAt` 只表示暂停开始时间，用于展示暂停持续多久；暂停到继续之间的时间不得写入 `eatingAccumulatedSeconds`。
- 继续吃饭时清空 `eatingPausedAt`，重新写入 `eatingStartedAt`，并沿用暂停前的 `eatingAccumulatedSeconds`。
- 只有点击“结束吃饭”才写入 `eatingEndedAt` 并进入再次切换冷却；后台配置 `eating_reentry_cooldown_min` 控制冷却分钟数。
- 吃饭冷却期和累计时长上限由后端接口强制校验；前端乐观切换失败时应回滚状态并提示剩余等待时间或累计上限。
- 吃饭中计时超过配置阈值后需要进行超时提醒，默认阈值为 30 分钟；提醒基于 `eatingAccumulatedSeconds + 当前吃饭段时长`。
- 助理端“当前任务状态”区域中，吃饭状态展示优先级高于暂停任务卡片；吃饭只改变在线/子状态展示和派发可用性，不自动恢复、完成或取消原暂停任务。
- 暂停吃饭回到在线后，如果 `eatingAccumulatedSeconds > 0` 或 `eatingPausedAt` 存在，且 `eatingEndedAt` 为空，当前任务状态区应保留低高度“吃饭已暂停”状态条；这只是展示口径，不新增业务状态，也不影响在线派发候选判断。
- 结束吃饭并恢复在线后，原暂停任务仍可继续。

#### 楼座与当前服务区域

- 助理可以在非进行中状态下切换工作楼座/大区；执行中任务不得直接切换工作区域。
- 摄影师工作台楼座/场地切换只是本页临时位置，不 PATCH 人员管理登记字段，也不移动身份切换窗口里的摄影师登记分组。
- 快捷下单使用摄影师工作台临时位置作为任务地点；如果当前楼座没有可用默认场地或用户未选择场地，不得硬编码到固定房间号，应提示无法创建或要求选择地点。
- 摄影师切换到非登记楼座时，头像名片默认显示该区域的公共区域；切回人员管理登记本楼座时，优先显示自己的办公室。任务创建必须以当前工作台位置为准，而不是登记资料。
- 助理当前服务区域使用 `activeBuildingId / activeRoom`；人员管理里的 `buildingId / currentRoom` 继续表示登记楼座/办公室。
- 助理切换楼座只写入当前服务楼座 `activeBuildingId`，不自动把该楼座默认公共区域写入 `activeRoom`；空闲态头像名片默认只显示楼座，不额外显示公共区域。
- 助理接了任务且处于进行中/暂停中时，头像名片才显示具体任务房间；待就位或空闲服务楼座不应把公共区域伪装成具体房间。
- 助理切换楼座影响派单、身份切换窗口助理分组、地图助理筛选和协作候选；登记楼座/办公室不变。
- 后台人员管理编辑只更新登记字段；只有显式传入 `activeBuildingId` 时，才按助理当前服务区域切换处理。
- 助理处于普通待就位状态时若切换楼座，说明原派发关系不再可靠，系统需要释放/重派相关待就位任务。
- 助理楼座切换不再作为右侧独立按钮列展示。
- 助理楼座切换入口挂在头像名片的地址/楼座文字区域下方。
- 只有进入地址区域时，才展开楼座切换菜单。
- 展开菜单层级需高于地图和任务面板，避免被中心地图容器裁切。
- 助理空闲时，头像名片区域优先展示当前大区/楼座信息；若仍在登记楼座，则展示“楼座 + 默认棚位/工位”。
- 助理切换到非登记楼座或公共大区时，头像名片区只显示切换后的大区名称，不显示原登记工位。
- 助理切回本人登记工位所在楼座时，恢复显示原本工位/默认棚位。
- 助理可切换区域列表只展示前面的大区域，不展开到具体棚位/房间层级。

### 9.5 多人协作

#### 协作资格与开关

- 多人协作不再按任务时长限制；1-5 分钟短任务如果现场需要多人共同完成，也可以启用协作。
- 协作规则按楼座/大区配置是否开启；大区关闭新增协作后，禁止新增协作助理，但保留已有协作展示，并允许移除已有协作成员。
- 当前区域公共队列达到自动关闭阈值时，新增协作入口关闭；队列拥堵自动关闭只影响新增协作，不影响已有协作任务继续展示、移除或完成。

#### 协作人数与参与者

- 多人协作人数上限由后台配置控制，自动派发的主助理计入人数上限。
- 后台配置的“最多 N 人”指任务总参与人数，包含主助理；可额外选择的协作人数 = 总上限 - 当前主助理与已有参与者。
- 当摄影师选择超过上限的协作助理时，超出者不可选，并用清晰提示说明超过上限。
- 主助理和协作助理统一抽象为任务参与者：主助理为 `primary`，协作助理为 `helper`。
- 多人协作下，每个助理独立计算待就位、执行中、暂停、完成状态。
- 某个协作助理可以中途完成自己的部分并离开，其他助理继续执行。
- 只有所有参与助理都完成后，摄影师侧任务才算整体完成。
- 协作中的助理不应被自动派发新任务，也不应进入执行中插单或待就位让行候选池。
- 协作助理端可以看到自己的协作任务状态，但不能操作主助理的开始、完成、暂停按钮。
- 摄影师和当前主助理可以维护协作名单；协作助理不能继续添加其他协作助理。
- 新增协作候选必须满足：同任务区域、在线、空闲、无活跃主任务、无活跃协作参与记录。
- 协作候选按助理当前服务楼座判断，和后端 `activeBuildingId ?? buildingId` 口径一致。
- 多人协作任务不能被执行中插单或待就位让行抢占；仍有活跃副助理的主任务不进入插单候选，协作副助理也因活跃参与记录不进入派发/插单候选池。

#### 协作展示与统计归属

- 协作助理暂停时，地图上保留该助理在任务位置的暂停头像，并按该助理自己的参与记录计算暂停耗时和超时。
- 已完成列表中，多人协作完成的任务显示多个助理姓名；多人协作完成的状态标签使用蓝色“多人协作完成”。
- 任务类型面板的完成归属只用于该面板展示：多人协作完成任务只把这 1 单记到主助理身上，协作助理不重复计数。

### 9.6 助理端交换/移交

- 交换/移交入口位于助理端「当前任务状态」右上角，仅当前主助理可见。
- v1 支持三种路径：无论目标助理空闲还是正在执行/暂停任务，都先创建 `confirming` 待确认请求；必须由目标助理本人确认后才会生效。
  - 空闲移交 `handoff`：目标确认后进入 `ready_to_takeover`，目标助理显示待就位/待接替；原助理仍保持当前任务负责人和工作状态，直到目标助理点击开始接替时才切换 primary 并释放原助理。
  - 马上互换 `swap + pause_and_go`：目标助理选择“暂停并前往”后，同一事务内结算两条任务当前工时、互换两条任务的 primary 负责人，两条任务都回到 `waiting`，双方到位后各自点击开始。
  - 结束后前往 `swap + after_complete`：目标助理选择“结束后前往”后，请求进入 `pending_after_complete`；候选助理原任务完成且原任务仍未完成时，请求进入 `ready_to_takeover` 并提醒原助理；候选助理点击开始接替时，才把原任务切给候选助理待就位并释放原助理；若原任务先完成，请求失效。
- 候选头像展示当前区域除自己以外的所有助理；不可选头像保留展示，并通过气泡提示离线、吃饭/休假、不同区域、已有待就位任务、任务不可移交等原因。
- 移交接替必须结算旧主助理当前执行片段工时；旧 primary 作为该任务的已完成参与记录保留，写入 `completed/completedAt/leftAt/effectiveWorkSeconds`，新助理写入 `primary/waiting`，任务保留 `startedAt` 并回到 `waiting` 等待新助理就位。
- 移交/互换后的综合分按参与记录分别结算：原助理已完成的工作片段和接替助理最终完成的工作片段都属于同一任务的完成贡献，分别进入个人综合分。
- 交换/移交使用 `TaskAssistantTransferRequest` 持久化状态：`kind` 区分 `handoff` / `swap`，`responseMode` 记录 `pause_and_go` / `after_complete`，`counterpartTaskId` 记录互换时候选助理原任务；`confirming` 表示等待目标确认，`pending` 保留为历史/兜底等待状态，`pending_after_complete` 表示目标已确认结束后前往，`ready_to_takeover` 表示目标已确认且正在待就位/待接替，点击开始后才真正切换主负责人。
- 目标助理确认手动交换/移交后，不再额外生成 `standbyReassignmentNotice` 二次提醒；空闲目标确认后直接进入待就位。
- 熨烫任务、外模跟拍协助任务、插单链路任务和多人协作任务 v1 暂不支持交换/移交。
- 第一版 `handoff` 和 `after_complete` 都通过 `ready_to_takeover` 收口：候选助理点击开始接替时才切换负责人并释放原助理。

### 9.7 工作台、地图、Dock 与备注展示

#### 中心地图与区域任务数据

- 中心地图区是当前区域工作容器，包含「楼层平面图」和「任务数据」两个页面。
- 楼层平面图页保留地图拖动、缩放、头像标记和状态图例；拖拽、缩放不应被自动刷新覆盖。
- 任务数据页面用于解释当前区域运转情况，不改变左侧「我的任务」和公共队列的业务归属。
- 地图容器视觉应与任务列表保持一致：圆角、半透明、柔和边框、毛玻璃质感。
- 地图底部图例与右侧助理 Dock 使用同一套状态口径：空闲中、待就位、进行中、已超时、已下线。
- 同一助理可以有多个地图状态标记：主头像、待就位让行灰头像、暂停灰头像、紧急插单待处理蓝脉冲；这些状态可以同时存在。
- 同一房间多名助理或多类标记应横向错开，悬停时提高层级，避免头像互相覆盖。
- 地图头像正常页面下保持可读的原始视觉尺寸；当页面窗口缩小时，头像按地图容器宽度适当缩小。
- 地图缩放时，头像不与底图同倍率暴涨，也不完全固定到显得过小；采用部分反向缩放，让地图放大时头像也略微变大但保持可控。
- 公共区域标签与助理头像使用独立缩放口径：标签在缩小窗口时仍需保持可读，不能为了适配把文字压得过小。
- 区域数据面板在中窄桌面宽度下应收紧圆角、内边距、字体、统计数字和栅格间距；必要时隐藏饼图，只保留任务类型列表，避免统计、公共队列、任务类型和助理排行互相挤压。
- 自动适配视图需要区分“系统模式切换”和“数据刷新”：用户手动拖拽/缩放地图后，轮询刷新不应覆盖 `zoom/pan`；只有楼座切换、抽屉开合等明确模式变化才重置手动状态。
- 右上角熨烫头像与地图头像是两个并行状态维度：右上角表达机器接续/准备/排队，地图表达助理所在任务房间，二者不互斥。
- 身份切换面板和右侧 Dock 需要随轮询同步 `allProfiles` 与当前 `profile` 的实时字段，包括在线状态、子状态、工作状态、当前服务楼座/房间和吃饭计时字段；不能只用初始快照展示在线状态。

#### 任务状态与可行动入口

- 任务列表中的暂停状态分为“手动暂停中”和“插单暂停中”；暂停标签可显示等待动效。
- 助理“我的任务”列表中的待就位、进行中、暂停中等状态标签只读，不触发开始、完成、暂停或恢复；助理任务操作集中在“当前任务状态”区域，只有已完成任务可通过状态标签进入添加登记/查看登记。
- 当助理同时存在多个可行动待就位任务时，当前任务状态栏应展示多个可点击入口，让助理选择先开始哪一条；普通待就位和 `notified` 准备熨烫都可进入可选列表，`waiting_machine` 等待熨烫机不可点击开始。
- 当当前区域有空闲熨烫机且存在可立即开始的熨烫任务时，助理端当前任务状态区域应给出明确提示：「当前熨烫机空闲，建议优先承接熨烫任务」。该提示只用于建议优先级，不绕过后台派发、机器使用权和任务归属规则。
- 当前任务状态不能把多个可行动任务压成单个 `current`；界面要优先展示助理下一步能实际操作的任务。

#### 备注与知悉

- 摄影师备注输入提示使用示例文案，例如 `如：需要带一个道具...`。
- 如果任务已经在进行中，摄影师不能再新增或修改备注信息。
- 地图上的头像点击只用于查看或定位状态，不应打开备注编辑入口。
- 右侧 Dock 和地图头像里的备注长文本最多显示两行。
- 摄影师在右侧 Dock 点击助理头像打开备注时，备注框贴近头像显示，并做屏幕边缘限制。
- 助理任务状态栏不直接显示备注正文；助理收到带备注任务时，在页面中心弹出备注知悉浮窗，展示摄影师、房间、任务类型、选择时长和备注正文。
- 备注知悉浮窗应包含任务类型、摄影师选择的时长和备注正文，尺寸需要适合现场快速阅读，标题、正文和按钮不能过小。
- 助理点击「已知悉」后关闭当前备注浮窗，同一条任务备注在当前页面不应立即重复弹出。

### 9.7 任务数据、统计与排行

#### 区域任务数据看板

- 区域任务数据页展示当前区域任务概览、区域进行任务、助理状态和任务类型占比，不改变左侧「我的任务」和公共队列的业务归属。
- 桌面窗口缩小时，区域任务数据面板先进入中间态适配：收紧圆角、内边距、统计数字、列宽和间距，而不是等到手机断点才变化。
- 中窄桌面宽度下，如果下方空间不足，可以隐藏任务类型饼状图，只保留任务类型列表，避免任务类型、统计数字和助理排行互相挤压。
- 桌面主工作区顶部和底部边距保持一致；当前口径为 `top: 36px`、`bottom: 36px`。
- 区域任务数据看板中，除「今日已完成」外，正在进行中、等待就位中、当前已超时、公共队列中、离线人员都支持展开人员气泡。
- 摄影师身份查看任务型指标时，优先显示发布任务的摄影师头像；助理身份查看实时人员型指标时，优先显示助理头像。
- 「公共队列中」始终显示队列任务发布摄影师头像，不随当前身份改变。
- 「当前已超时」气泡必须区分任务超时和吃饭超时。
- 摄影师视角的区域“当前已超时”数字，按当前区域任务统计；当前区域任务是否超时统一通过 `overtimeMinutesBeyondSlot(task, nowMs)` 判断。
- 统计页口径和实时工作台口径必须分开：后台已完成超时看已完结任务，工作台当前超时看正在进行/暂停中的实时超时。
- 助理视角的「等待就位中」需要包含公共队列里已经分配助理、但助理尚未就位的任务。
- 摄影师视角的超时数字和超时气泡明细必须同源，不能出现主数字有值但气泡为空。

#### 任务类型展示

- 任务类型面板分为发布数据和完成数据两套口径：上方饼图展示发布分布，下方列表展示已完成分类单数。
- 任务类型顺序与快捷预约保持一致：`手持`、`服装穿戴`、`手工DIY`、`熨烫`、`其他`。
- 统计页、人员标签和综合分明细展示需要把外模协助/跟拍任务从「其他」里单独显示为「外模跟拍协助」；快捷预约入口仍可保留在「其他」分类下。
- 统计页和人员标签可使用简化口径：`手持`、`穿戴`、`手工`、`熨烫`、`外模跟拍协助`、`其他`。
- 归并规则：名称包含「手持」归手持；包含「穿戴」或「服装」归穿戴；包含「手工」或「DIY」归手工；包含「熨烫」或「熨」归熨烫；其他无法识别归其他。
- 摄影师名字后方可展示任务类型短标签；多个队列任务类型用 `/` 分隔，标签应克制，不能导致人员气泡过宽或拥挤。
- 鼠标移动到任务类型饼图时，需要显示对应任务类型、单量和占比；信息位置应贴近当前扇区，并避免被其他容器遮挡。

#### 后台数据统计运营看板

- 后台「数据统计」定位为运营看板，支持今日、本周、上周、本月、上月、自定义日期筛选。
- 统计页以筛选后的统一任务集合为基础，所有总览、排行、趋势和明细应使用同一套筛选口径，避免上方数字和下方明细不一致。
- 后台统计默认按任务 `createdAt` 归入发布日期，趋势图可同时按 `completedAt` 展示完成量。
- 自定义日期的结束日期按自然日包含处理，例如选择 `6/18` 应包含 `6/18 00:00:00` 到 `6/18 23:59:59` 的任务。
- 数据统计支持三类组织范围：全部范围、按助理部门、按楼座。
- 助理部门直接对应人员资料 `department` 字段；核心部门为一部、二部、全域优化组。
- 楼座统计口径优先使用任务 `locationBuildingId`，缺失时回退摄影师所属楼座。
- 助理排行综合分公式：实际服务时长（1 小时 = 1 分）；「外模跟拍协助」属于「其他」类专项任务，快捷预约等级为 P6，并按实际服务时长分值乘以 0.8 系数。
- 助理排行以已完成任务为基础；主助理和协作助理都计入完成贡献。
- 实际服务时长使用任务/参与者有效工作时长；等待、待就位、暂停、插单占用等非真实服务时间不计入。
- 完成任务数量和跨区次数不再作为额外补偿加分；同分时可作为辅助排序或运营观察信息。
- 点赞/点踩在助理排行中展示，但不参与当前贡献值计算。
- 前三名皇冠只作为视觉标识，不影响分数、排序或派发逻辑。
- 摄影师发布情况展示发布总任务、已完成任务数、已完结超时任务数和发布最多的任务类型。
- 统计页「超时任务」只统计已完结任务中 `completedAt > estEndTime` 的数量，不等同于主工作台实时超时。
- 数据统计底部任务明细需要支持搜索摄影师姓名、搜索助理姓名、中文姓名首字母快速搜索、按部门筛选、按楼座筛选、按任务类型筛选、按任务优先级筛选。
- 姓名搜索同时匹配摄影师、主助理和协作助理；首字母搜索作为快捷辅助，不要求完整拼音库。
- 明细表头应固定在明细容器顶部，滚动明细内容时表头不离开容器。
- 区域「今日已完成」数字可作为进入当前区域任务数据弹窗的入口；打开时默认回到当天数据，弹窗标题使用“xx区域 任务数据”。
- 区域任务数据弹窗只承载当前区域完成统计、趋势、任务类型占比和完成明细，不改变后台运营看板的全局筛选口径。

### 9.8 后台空间、任务逻辑与配置管理

#### 空间与公共区域

- 后台空间管理负责楼座、房间、公共区域和熨烫机基础资料维护。
- MapEditor 支持房间标记、额外场地标记和裁剪区域编辑；公共区域支持多点框选，并直接显示在主工作台地图上。
- 后台编辑公共区域时，点击空白地图新增顶点，拖动已有顶点移动该点，点击已有顶点不能穿透新增点。
- 空间管理地图编辑区需要给公共区域框选、房间点位和熨烫机管理留出足够空间；熨烫机不再作为可拖拽点位。
- 主工作台展示公共区域时，以半透明区域颜色为主，不需要明显边框；中间文字标签颜色可以淡一点。
- 后台空间管理编辑公共区域时，地图越放大，标记点位数字圈视觉上应越克制，不能遮挡主体信息。
- 楼座列表放在地图下方，并与房间列表平齐；右侧新增场地/公共区域列表高度尽量与地图编辑区底部齐平。
- 右侧栏顺序以管理频率为准：公共场地/其他场地在上，熨烫机管理在下。

#### 熨烫机管理

- 熨烫机管理归属于后台「空间管理」：可按楼座/区域手动添加熨烫机数量，默认按数字序号生成名称。
- 熨烫机状态只允许在“正常运行 / 维修不可用”之间调整。
- 后台仍保留熨烫机新增、删除、正常/维修状态管理；不展示或维护前台地图坐标。
- 熨烫机管理容器使用“添加熨烫机”操作，点击一次新增 1 台，不再提供“新增台数”输入。
- 熨烫机管理列表展示 `熨烫机1、熨烫机2...`，不显示地图坐标和“可拖拽定位”类文案。

#### 任务逻辑与全局配置

- 后台「任务逻辑」管理任务类型、优先级、时长范围、是否可插单、可插单离场时间和全局配置。
- 熨烫机接续规则中可调：准备窗口、空出确认倒计时、机器使用权保护时间。
- 待就位超时多久更换派发助理是独立配置，不与熨烫机接续规则合并。
- 空间管理页维护工作台页面背景素材，保存到 `system_config.workbench_page_background`；摄影师/助理工作台读取该配置作为整页背景，未配置时使用默认背景。
- 后台配置项的默认值与范围见 §7。

#### 视觉可读性原则

- 项目整体保留半透明毛玻璃 UI 和自定义背景图；可读性问题不能靠完全取消透明效果解决。
- 优先通过全局柔和遮罩、提高玻璃容器不透明度、提升文字对比度、给关键 tooltip/按钮/表格头部独立可读底色来解决阅读性。
- 后台背景不应出现网格纹路或过重灰雾感；容器透明度需要在保留玻璃感和文字清晰之间取平衡。
- 统计面板、排行、任务类型等固定区域应优先回到 grid/flex 轨道内受布局约束；不要用脱离布局的 `absolute + min-width` 强行顶层显示，避免窄窗口覆盖相邻内容。
- 工具提示、明细气泡、备注框等浮层应使用页面顶层浮层并按视口夹紧定位；带 `transform` 动画的容器内部不应直接渲染 `position: fixed` 浮层，避免坐标错位。

### 9.9 移动端、部署与生产化路线

#### 终端使用场景与信息架构

- 未来真实使用场景按“园区内多人协同”设计：助理以手机浏览器/PWA 为主，摄影师以网页端为主，管理人员同时需要桌面后台和必要的手机端入口。
- 手机端不是桌面页面的简单缩放，而是同一业务逻辑下的移动端专用信息架构；桌面端保留鸟瞰地图、左侧任务面板和右侧 Dock，小屏端使用工作台移动端布局。
- 移动端适配采用“方案 B”：保留同一页面和业务数据逻辑，在小屏下渲染工作台移动端专用布局；后台移动端后续按卡片列表、分组表单和全屏编辑模式逐步重排。
- 工作台移动端导航采用“C 类混合方案”：顶部保留身份、位置、在线状态和高优先级提醒，底部导航承载主功能。
- 工作台移动端底部导航固定四项：当前、地图、队列、我的。
- 手机端主屏第一层只放当前身份的高频动作，其他信息进入底部导航、抽屉、弹窗或二级页面。
- 助理移动端第一层优先展示当前任务、待开始任务、继续任务、完成任务、短暂离开、吃饭/休假/在线状态恢复等可行动入口。
- 摄影师移动端第一层优先展示快捷发单、时长选择、当前发布任务、任务状态与反馈入口；地图和区域队列放到底部导航。
- 管理身份在移动端必须能访问摘要、后台入口、身份切换和工作台查看入口；后续后台移动端不能只展示统计摘要，必须能进入新增、编辑、删除、保存等完整管理流程。
- 手机端不能依赖 hover；桌面 hover 菜单、浮层和详情在移动端必须改为点击、底部弹层、抽屉或全屏弹窗。
- 手机端触控目标不小于 44px，主操作按钮应处于单手可触达区域，避免把地图常驻在首屏挤占任务操作区。
- 移动端视觉必须与桌面工作台保持一致：沿用浅灰/背景图、玻璃卡、橙色主操作和蓝绿红灰状态色，不另起一套新皮肤。
- 移动端允许降低毛玻璃叠层数量和装饰噪音，但不改变派单、插单、熨烫、吃饭、离线、协作、后台配置等业务规则。
- 平板竖屏优先走移动工作台；桌面宽度继续使用桌面鸟瞰图和多面板布局。

#### 手机真机身份与布局一致性

- 手机真机、电脑 `localhost`、局域网 IP 是不同 origin；`localStorage` 不共享，不能依赖电脑本地缓存来决定手机端当前身份。
- 工作台必须支持通过 URL 承载身份：`/photographer?profileId=...` 或 `/photographer?employeeId=...`。
- URL 身份优先于本地缓存；URL 命中后写入手机本地 `currentProfileId/user`，便于后续同一设备直接访问。
- 页面成功识别当前身份后，应把地址栏统一同步为 `?profileId=...`，让电脑端复制出来的链接天然可用于手机端访问同一身份。
- 没有 URL 身份、没有本地缓存、也没有可匹配登录身份时，手机端必须显示“选择身份继续”，不得默认选第一个人员，也不得落到管理工作台兜底。
- 全站移动适配必须显式输出 `viewport width=device-width, initial-scale=1`，确保真机浏览器和电脑设备模拟使用同一个 CSS 断点。
- 移动端首帧不要用 `window.location`、`localStorage`、窗口尺寸等客户端专属值直接决定与服务端不一致的文案；应先显示稳定加载态，挂载后再解析身份，避免 hydration mismatch 造成点击事件失效。
- Next 16 开发服务用局域网 IP 给真机访问时，需要在 `next.config.ts` 配置 `allowedDevOrigins` 并重启；否则可能出现服务端 HTML 能出来，但客户端接管、接口刷新或点击事件异常。
- 手机端访问内容与电脑端预览不一致时，优先按以下顺序排查：访问地址是否带 `profileId`、是否命中同一接口数据、是否输出正确 viewport、是否存在 hydration mismatch、是否有遮罩或高层级元素吞掉点击。

#### PWA 与 App 封装路线

- 当前阶段优先做 PWA，而不是立即同时开发 iOS、Android、鸿蒙三套原生 App。
- PWA 目标是让同一套 Web 工作台“像 App 一样”使用：图标、全屏打开、添加到主屏幕、移动端适配、后续可增加离线壳和安装提示。
- 苹果、安卓、鸿蒙等手机端都优先访问同一套 PWA 页面，保证业务逻辑、页面结构和数据状态一致。
- 当需要上架、原生推送、扫码、相机、定位、蓝牙、系统级权限或更强离线能力时，再基于同一套 Web 页面使用 Capacitor、WebView 或鸿蒙 ArkWeb 壳封装。
- App 壳封装不得另写一套业务规则；壳只负责容器、权限、启动图标、推送和系统集成，核心任务流程仍由 Web/PWA 页面承载。
- 正式 PWA/App 使用必须部署到稳定 HTTPS 域名；`192.168.x.x:3000` 只用于开发和园区内测试，不作为长期生产访问地址。
- 员工手机端长期入口应从“复制开发 IP 链接”升级为“登录/身份绑定 + 固定 HTTPS 地址 + 添加到主屏幕”。

#### 百人并发与数据库路线

- 目标使用规模按摄影师、助理、后台人员合计上百人同时在线规划。
- 当前 SQLite 适合便携开发、小团队试运行和单机演示；上百人同时创建、开始、完成、转派任务时，SQLite 单写入特性仍可能产生写锁排队。
- 第一阶段性能优化优先减少不必要写入和高频读接口副作用：高频读取接口不应每次触发全局清理、状态同步、动态提权和全局派单扫描。
- 清理、状态同步、动态提权、扫队列等维护任务应逐步从高频 `GET` 请求中拆出，改为明确触发、节流触发或后台 worker 周期执行。
- PostgreSQL 迁移作为生产化阶段单独推进，不与移动端 UI 或普通业务修补混在一起。
- 长期上百人生产使用时，应迁移到 PostgreSQL，并把调度/维护任务放到独立后台 worker 或定时任务中，避免由用户页面请求承担全局维护压力。
- 迁移 PostgreSQL 后仍需保留同一业务口径：任务状态、参与者、熨烫机队列、摄影师发布上限、待就位转派和统计口径不得因数据库迁移而改变。
- 局域网内部使用也应按生产系统治理：稳定服务器、固定域名、HTTPS、备份、日志、监控和清晰的启动/停止方式。
- 百人压测必须记录接口延迟分位数、SQLite 写锁/忙等待、`sweepWaitingTasks()` 单例防抖命中、Node CPU/内存、接口响应体大小和轮询频率；只看页面是否能打开不足以判断生产可用性。
- 当前 `prisma/dev.db` 仍是本地 SQLite 文件，发布时不得默认提交或覆盖 Mac mini 生产库；开发库、生产库和备份库要有清晰路径边界，生产库替换必须先停服务再备份。
- 依赖安全升级不得使用粗暴 `npm audit fix --force`；框架补丁、Prisma、Socket.IO、Excel 解析库等必须分批升级、分批验证。
- 后台 Excel 人员导入使用 `xlsx` 时必须视为高风险入口：先限制后台权限、上传大小、sheet/行数/字段白名单，再评估替换维护更活跃的解析方案。

#### Mac mini 本地固定访问部署

- 园区内固定访问的第一阶段推荐使用 Mac mini 单机生产部署：`npm run build` 生成生产产物，`npm run start -- -p 3000 -H 0.0.0.0` 对局域网提供服务。
- Mac mini 应在路由器或网络设备中绑定固定局域网 IP；电脑端和手机端统一访问 `http://<固定 IP>:3000/photographer`，有内网 DNS 时可进一步绑定本地域名。
- macOS 长期运行优先使用 `launchd` 托管进程，实现登录后自启、异常自动重启、日志落盘和明确的停止/重启命令；PM2 仅作为后续需要更友好进程面板时的备选，不作为当前默认依赖。
- Mac mini 生产入口与开发一键启动器分离；一键启动器继续用于开发/演示，长期运行不得依赖 `next dev`。
- 当前 SQLite 数据库仍使用本地文件 `prisma/dev.db`；生产试运行阶段必须保持单机单服务实例写入，不要同时运行多个开发/生产进程写同一个库。
- SQLite 备份放入项目 `database-backups/`；服务启动、重大配置调整、人员批量导入前应备份，恢复时必须先停止生产服务再替换数据库文件。
- 本地 HTTP 固定 IP 可用于园区试运行；正式 PWA/添加到主屏幕推广前，应准备稳定域名、HTTPS 证书或内网反向代理，并把 `NEXT_PUBLIC_APP_URL` 调整为最终固定地址。
- Mac mini 部署操作手册以 `MAC_MINI_DEPLOY.md` 为准；涉及业务口径、派单规则或数据库迁移时，先交由老王/小刘确认，不由小张在部署任务中顺手修改。

#### 一键启动与开发环境边界

- 一键启动器定位为本地开发/演示工具，不是生产部署方式。
- 一键启动器必须单实例、低风险、可复用已有服务；重复双击不得重复拉起多个 Next dev 服务。
- 一键启动入口包括 `一键启动项目.command` 和兼容入口 `打开项目.command`。
- 一键启动默认打开 `/photographer` 工作台；端口以当前脚本配置为准，开发期可固定在 `3000` 或 `3001`，但不能同时拉起多个端口造成混乱。
- 启动器应使用 lock 文件或端口健康检查识别已运行实例；发现僵尸 lock 或端口占用但页面不可访问时，应清理旧状态后再启动。
- 启动器发现端口已有本项目服务时应直接复用并打开页面；发现端口被其他程序占用但项目无响应时，只提示占用进程，不自动杀进程或另开端口。
- 启动器需要识别并清理 PID 已不存在的 `.next/dev/lock`；如果 lock 指向的 Next 进程仍活着，则等待该服务就绪并复用，不重复启动。
- 启动器日志应截断重建，避免长期累积；状态锁放在临时目录，退出或中断后清理。
- 开发服务应优先在终端前台运行，关闭终端即可停止；如果使用后台方式，必须提供清晰日志路径和卡死恢复逻辑，避免用户无法发现异常进程。
- 启动器健康检查应使用轻量接口，例如 `/api/config`；不要在等待循环中反复请求 `/photographer` 这类重量页面。
- Next 16/Turbopack 项目需要固定 `turbopack.root` 到真实项目目录，避免开发服务监听整个移动硬盘或上级工作区造成内存和文件监听压力。
- 开发期可以限制 Node/Turbopack 内存上限，但正式生产部署应使用构建产物和受控进程管理，而不是依赖开发服务器。
- 启动前需要清理 `.next` 下 macOS 可能生成的 `._*` 伴生文件，避免移动硬盘上的 Turbopack 缓存解析失败。

#### 人工提权审批

- 摄影师可以对自己发布的单条 `waiting/paused` 任务发起人工提权申请，申请端固定申请到 P1。
- 后台「审批管理」可配置人工提权申请开放范围，默认 P3；此处“P3以上”按业务口径表示 P3/P4/P5 可申请，“P2以上”表示 P2/P3/P4/P5 可申请。
- 提权申请理由必填；已经 `executing` 或 `completed` 的任务不允许申请提权。
- 人工提权申请进入后台「审批管理」，由管理审核。
- 管理审批时可以调整实际批准优先级，例如 P4 申请 P1，但按现场情况批准为 P2。
- 审批通过后才更新任务 `priority`，并写入 `escalatedAt` 和 `escalatedFromPriority`，随后触发一次任务维护，让新优先级进入既有派发/插单流程。
- 审批驳回只关闭申请，不改变任务优先级。
- 人工提权申请使用独立表 `TaskPriorityUpgradeRequest` 记录申请人、申请理由、原优先级、目标优先级、批准优先级、审批人和审批时间；不要复用任务自动提权字段承载待审批状态。

### 9.10 已完成任务异常登记 / SKU 追踪

- 助理已完成任务可以补充「异常登记 / SKU 追踪」，用于记录超时过长、耗时明显异常、需要复盘的任务。
- 异常登记不参与派单排序、不改变任务状态、不改变任务优先级，只作为后台数据中心分析维度。
- 登记内容：
  - SKU 必填；
  - 图片和补充说明可选，补充说明限 100 字；
  - 图片最多 3 张，单张不超过 8MB；
  - 一条任务只保留一条登记，可再次编辑覆盖；
  - 手机端登记图片输入应支持拍照或上传。
- 数据模型使用独立表 `TaskCompletionRegistration`，不要把图片、说明、SKU 直接堆进 `BookingTask` 主表。
- 图片保存到 `public/uploads/task-completions`，数据库只保存图片 URL 数组，避免 SQLite/未来数据库被图片内容撑大。
- 登记人必须是该任务主助理或协作助理；后台统计只读展示。
- 后台数据中心需要支持按登记状态、SKU、摄影师、助理、任务类型、是否超时筛选，以便复盘异常耗时来自哪些 SKU 和人员组合。

### 9.11 历史归档索引

- `tasks/project-operation-logic-2026-06-11.md`：历史归档，保留 2026-06-11 讨论和决策上下文，不再作为当前规则入口。
- `tasks/project-operation-logic-2026-06-18.md`：历史归档，保留 2026-06-18 工作台、统计、备注、熨烫机和空间管理相关决策上下文，不再作为当前规则入口。
- `tasks/todo.md`：只记录任务计划、执行进度和评审。
- `tasks/lessons.md`：只记录经验教训。
- 若历史归档与本文冲突，以 `摄影助理自动派单系统.md` 当前分类规则为准。

---

## 附录：关键文件索引

| 文件 | 说明 |
|------|------|
| `prisma/schema.prisma` | 数据库模型定义 |
| `src/lib/scheduler.ts` | 自动派单调度器（核心逻辑） |
| `src/lib/prisma.ts` | Prisma 客户端单例 |
| `src/lib/socket.ts` | Socket.io 实时通信 |
| `src/types/index.ts` | `PRIORITY`、`SCHEDULER_CONFIG` 默认值、`SOCKET_EVENTS`、`STATUS_COLORS` |
| `src/lib/eatingPresence.ts` | 吃饭状态计时、冷却与可切换性判断 |
| `src/lib/taskEffectiveTime.ts` | 有效执行时长累计与超时计算 |
| `src/app/api/auth/login/route.ts` | 登录 |
| `src/app/api/tasks/route.ts` | 任务 CRUD 接口 |
| `src/app/api/tasks/[id]/route.ts` | 单任务操作接口 |
| `src/app/api/tasks/sweep/route.ts` | 手动派单触发 |
| `src/app/api/profiles/route.ts` | 用户管理接口 |
| `src/app/api/profiles/[id]/route.ts` | 用户更新接口（含派单触发） |
| `src/app/api/buildings/route.ts` | 楼座管理接口 |
| `src/app/api/buildings/[id]/route.ts` | 楼座更新接口 |
| `src/app/api/buildings/[id]/rooms/route.ts` | 房间管理接口 |
| `src/app/api/categories/route.ts` | 任务类型管理接口 |
| `src/app/api/config/route.ts` | 系统配置接口 |
| `src/app/photographer/page.tsx` | 摄影师工作台（2400+ 行） |
| `src/app/photographer/book/page.tsx` | 预约创建页 |
| `src/app/photographer/AssistantDock.tsx` | 助理侧栏组件 |
| `src/app/admin/page.tsx` | 管理后台 |
| `src/app/admin/components/MapEditor.tsx` | 地图编辑器组件 |
| `src/app/assistant/page.tsx` | 助理页面 |
| `src/app/leader/page.tsx` | 组长页面 |

---

## 10. 文档维护与后续补齐

- **§0** 已约定：逻辑变更应同步更新本文档；**运行时仍以代码为准**，本文档不被 Node/浏览器执行。
- 下列主题仍有细化空间，可在后续迭代按页或按组件追加子节：

| 主题 | 建议补充位置 |
|------|----------------|
| 摄影师工作台 `page.tsx` | 除 §6.2 地图外：轮询间隔、任务列表分组、`resolveAssistantTasks` 与 Dock 字段对照表 |
| Socket 客户端 | 各页是否 `join:building` / 订阅事件；与 HTTP 轮询关系 |
| `/stats` 与统计组件 | 查询口径、聚合维度 |
| 管理后台各 Tab | 与 §5 API 的字段级映射 |
| 组长页 / 助理页 | 实际实现与「原型」描述差异 |
| 自动化测试 | 测试目录与覆盖场景索引 |

**修订历史**：建议用 Git commit 关联文档变更；若需显式 changelog，可在本节前增加日期/摘要表。
