# 任务：右侧助理头像备注框定位

## 计划

- [x] 定位右侧助理头像 Dock 的备注点击入口。
- [x] 将点击头像时的屏幕位置传给备注弹窗。
- [x] 把备注弹窗从网页中心改为头像旁边的锚定浮层。
- [x] 浏览器验证备注框位置。

## 可选项

- 保留点击空白处关闭备注框。
- 对窗口边缘做位置限制，避免备注框超出屏幕。

## 评审

- 已将备注框从居中遮罩改为固定定位浮层，点击右侧助理头像时出现在头像左侧。
- 已保留点击空白处、取消、保存后关闭备注框的行为。
- 已对左右和上下边缘做 clamp，窄屏时备注框不会跑出视口。
- `npx tsc --noEmit` 通过。
- `npm run lint` 未通过，失败项为项目既有 lint 问题，与本次备注框定位改动无关。

## 追加需求：备注提示与长文本显示

### 计划

- [x] 将备注输入框占位提示改为示例文案。
- [x] 将右侧 Dock 备注长文本限制为两行显示。
- [x] 刷新浏览器验证输入提示和 Dock 两行显示。

### 评审

- 备注输入框提示已改为 `如：需要带一个道具...`。
- 右侧 Dock 和地图头像提示里的备注都使用统一的 `.note-two-line` 两行截断样式，并在元素上加了 `maxHeight` 兜底，确保最多显示两行。
- 浏览器验证：输入框 placeholder 为 `如：需要带一个道具...`，Dock 备注计算样式为 `max-height: 25px; overflow: hidden`。

## 追加需求：助理备注知悉弹窗

### 计划

- [x] 定位助理身份「当前任务状态」里的备注渲染。
- [x] 移除助理任务状态栏内的备注文本。
- [x] 助理身份收到带备注任务时显示居中毛玻璃浮窗。
- [x] 点击绿色「已知悉」后关闭当前备注浮窗。
- [x] 浏览器验证助理视角显示效果。

### 评审

- 助理身份的「当前任务状态」卡片已不再显示备注信息。
- 助理账号进入页面时，带备注任务会在页面中心弹出「摄影师备注」浮窗，背景为毛玻璃虚化。
- 点击绿色「已知悉」后浮窗关闭，同一条任务备注在当前页面会记为已知悉，不会立即反复弹出。
- 浏览器用助理账号 `WG6145` 验证通过。

## 追加需求：备注弹窗显示任务与时长

### 计划

- [x] 定位助理备注浮窗副标题。
- [x] 在副标题追加任务类型。
- [x] 在副标题追加摄影师选择的时长。
- [x] 浏览器验证弹窗文案。

### 评审

- 助理备注弹窗副标题已显示：摄影师、房间、任务类型、摄影师选择时长。
- 浏览器用 `WG6554` 验证示例：`徐磊 · 101-G室 · 手持 · 1-3分钟`。

## 追加需求：备注弹窗整体放大

### 计划

- [x] 定位备注知悉弹窗尺寸样式。
- [x] 将弹窗宽度、内边距、图标和字体整体放大约 50%。
- [x] 浏览器验证放大后的显示效果。

### 评审

- 备注知悉弹窗最大宽度从 `340px` 调到 `510px`，内边距、圆角、图标、备注文本和按钮同步放大。
- 标题字号从 `15px` 调到 `23px`，备注正文和按钮字号从 `14px` 调到 `21px`。
- 浏览器验证当前窄视口下标题 `23px`、正文/按钮 `21px`，弹窗按视口自适应显示。

## 追加需求：地图底部图例与 Dock 一致

### 计划

- [x] 定位地图底部状态栏代码。
- [x] 将地图底部图例改为 Dock 同款五项状态。
- [x] 将地图底部图例颜色改为 `DOCK_DOT` 同源颜色。
- [x] 浏览器验证地图底部与右侧 Dock 图例一致。

### 评审

- 地图底部图例已改为：`空闲中 / 待就位 / 进行中 / 已超时 / 已下线`。
- 地图底部图例颜色已改为复用 `DOCK_DOT`，与右侧 Dock 图例同源。
- 浏览器验证底部和右侧 Dock 均显示同一组状态。

## 追加需求：已完成任务隐藏备注

### 计划

- [x] 定位「我的任务」列表备注渲染区域。
- [x] 已完成任务不显示已有备注。
- [x] 未完成任务继续显示、编辑和新增备注。
- [x] 浏览器验证已完成卡片隐藏备注。

### 评审

- 「我的任务」列表中 `已完成` 任务不再显示已有备注。
- 未完成任务仍保留备注显示；摄影师视角未完成任务仍可新增/编辑备注。
- 浏览器验证：带备注的 `手持 1-3分钟` 已完成任务只显示任务信息和实际用时，不再显示备注。

## 追加需求：我的任务列表宽度对齐

### 计划

- [x] 定位「我的任务」列表宽度样式。
- [x] 移除滚动容器右侧固定留白，让任务卡片与上方任务栏同宽。
- [x] 浏览器验证任务卡片宽度对齐。

### 评审

- 已移除「我的任务」滚动容器右侧 `18px` 固定留白。
- 浏览器验证：当前任务状态内容区宽度 `216px`，我的任务卡片宽度 `216px`，左右边界一致。

## 追加需求：任务列表暂停类型与等待动效

### 计划

- [x] 定位「我的任务」列表中暂停状态标签渲染。
- [x] 根据任务父子关系区分「手动暂停」与「插单暂停」。
- [x] 在摄影师任务列表的暂停标签左侧加入三点循环等待动画。
- [x] 类型检查并浏览器验证显示效果。

### 评审

- `paused` 任务会按父子任务关系显示为「手动暂停」或「插单暂停」。
- 摄影师视角下，暂停标签左侧会显示三点循环跳动等待动画；助理视角不显示该动画。
- 当前数据校验：章若欣的暂停任务为「手动暂停」，徐磊的暂停任务为「插单暂停」。
- `npx tsc --noEmit` 通过；浏览器验证当前暂停任务标签已从「已暂停」变为「手动暂停」。

## 修正：摄影师暂停三点动画不明显

### 计划

- [x] 检查暂停标签中三点动画的可见性。
- [x] 将三点动画改为更明显的独立黄橙色圆点。
- [x] 修正空 `span` 未生成可见尺寸的问题。
- [x] 将暂停标签文案改为「手动暂停中 / 插单暂停中」。
- [x] 类型检查并浏览器复核。

### 评审

- 暂停标签文案已改为「手动暂停中 / 插单暂停中」。
- 三点等待动画改为独立黄橙色圆点，并修正空 `span` 未生成尺寸导致看不到的问题。
- `npx tsc --noEmit` 通过。
- 浏览器当前为管理身份且无暂停任务，因此当前页面无暂停标签可直接截图复核；有暂停任务的摄影师身份进入后会显示新文案和三点动画。

## 调整：任务列表状态标签文字放大

### 计划

- [x] 定位「我的任务」卡片状态标签字号。
- [x] 将状态标签文字调整到 `10px` 并保持标签紧凑。
- [x] 类型检查并浏览器复核。

### 评审

- 「我的任务」卡片状态标签字号已从 `8px` 调整到 `10px`。
- 保留 `leading-none` 和紧凑内边距，避免标签明显撑高任务卡片。
- `npx tsc --noEmit` 通过。

## Bug：≤30分钟手持单仍被插单

### 计划

- [x] 核查当前任务类型数据库配置与派单插单代码路径。
- [x] 修复待就位抢占路径漏检查「可被插单」的问题。
- [x] 后端统一强制 `maxDuration <= 30` 不可被插单，避免旧数据/前端显示不一致。
- [x] 修正分类保存接口，使保存时同步落库不可插单规则。
- [x] 回归验证并记录结果。

### 评审

- 根因：执行中插单会检查 `canBeInterrupted`，但待就位抢占路径 `interruptWaitingPreempt` 漏查，导致「待就位」状态的手持 5-15 分钟仍会让行紧急单。
- 另一个原因：数据库旧配置中 `手持 P2 5-15分钟` 的 `can_be_interrupted` 仍为 `1`，和前端“≤30分钟不可插单”的显示不一致。
- 已修复：派发逻辑统一通过 `taskCategoryCanBeInterrupted` 判断；执行中插单和待就位抢占都会读取并尊重任务类型配置。
- 已修复：分类新增/编辑接口会把 `maxDuration <= 30` 强制保存为不可插单，避免前端设置和后台派发再次不一致。
- 已同步当前数据库：所有 `maxDuration <= 30` 的任务类型已改为 `can_be_interrupted=0`。
- 已清理当前错误生成的插单关系；浏览器复核不再显示「已让行紧急单」，叶梦妮当前显示为执行中手持 5-15分钟。
- 验证通过：`taskCategoryCanBeInterrupted` 回归断言通过，`npx tsc --noEmit` 通过。

## 新功能：长时任务多人协作

### 计划

- [x] 梳理任务/助理派发与展示链路。
- [x] 新增协作关系数据模型与协作 API。
- [x] 让派发与状态同步识别协作助理，避免协作中继续被自动派单。
- [x] 在摄影师「我的任务」卡片增加协作按钮与协作助理弹窗。
- [x] 地图/Dock 展示协作助理在任务房间的协作状态。
- [x] 数据库迁移、类型检查与浏览器验证。

### 评审

- 新增 `task_collaborators` 表，用于记录任务与协作助理关系。
- 新增 `/api/tasks/[id]/collaborators`，摄影师可同步设置协作助理列表。
- 自动派单、空闲扫描、状态同步已排除协作中的助理，避免协作助理继续被派新任务。
- 主助理仍负责开始、暂停、完成；协作助理只作为参与者显示与占用。
- 摄影师任务卡片中，`maxDuration > 30` 的未完成任务会显示「添加协作 / 协作 N人」按钮。
- 协作弹窗可选择同楼座在线空闲助理，保存后 Dock/地图按同一任务房间展示协作状态。
- 已执行 `npx prisma db push` 和 `npx prisma generate`。
- API 回归验证：临时长时任务添加协作助理成功，移除成功，测试任务已删除。
- `npx tsc --noEmit` 通过；浏览器验证临时长时任务卡片出现「添加协作」入口，清理后页面恢复。

## 修正：协作助理端无任务状态提示

### 计划

- [x] 修正助理任务查询，让协作任务也返回到助理视角。
- [x] 在助理「当前任务状态」中为协作任务显示只读提示。
- [x] 防止协作助理误触主任务开始/完成/暂停按钮。
- [x] 类型检查并浏览器/API 验证。

### 评审

- 根因：`/api/tasks?assistantId=...` 原来只查主助理字段 `assistantId`，没有把 `task_collaborators` 里的协作任务返回给助理端。
- 已修复：助理任务查询现在返回主助理任务 + 当前协作任务。
- 助理身份如果是协作成员，会看到只读的「协作待就位 / 协作进行中 / 协作暂停中」状态提示。
- 协作助理不会看到主助理的「点击开始任务 / 点击完成任务 / 手动暂停」操作按钮。
- API 回归验证：临时任务添加协作助理后，用协作助理 ID 查询任务可以查到该任务；测试任务已删除。
- `npx tsc --noEmit` 通过；浏览器刷新无异常，Dock 已能显示协作中任务。

## 待确认：多人协作独立状态模型

### 目标

- 每个参与助理都有自己的就位、进行中、完成状态。
- 任意助理可完成自己的部分并离开。
- 只有所有参与助理都完成后，摄影师侧任务才算最终完成。
- 主助理不再独占任务完成权；主助理和协作助理都只是任务参与者之一。

### 建议方案

- [ ] 将任务参与者抽象为统一的 `TaskParticipant`：主助理 `primary`，协作助理 `helper`。
- [ ] 每个参与者独立记录 `waiting / executing / completed / left` 状态。
- [ ] 主任务 `BookingTask.status` 作为汇总状态：有人未完成则保持未完成，全部完成才进入 `completed`。
- [ ] 助理端只操作自己的参与记录：开始自己的部分、完成自己的部分。
- [ ] 摄影师端任务卡片展示每位助理的独立状态和整体完成进度。
- [ ] 自动派单只给主助理；摄影师手动添加协作助理后，该助理进入自己的待就位状态。

## 执行：多人协作独立状态模型

### 计划

- [x] 将 `task_collaborators` 从协作关系升级为任务参与者记录，主助理和协作助理都进入同一张表。
- [x] 后端任务操作改为按当前助理的参与记录开始、暂停、完成，并汇总主任务整体状态。
- [x] 摄影师端展示每位参与助理的独立状态；助理端只操作自己的任务部分。
- [x] 清理旧 `active` 协作状态，迁移数据库并回归验证。

### 可选项

- 保留主任务 `assistantId` 作为主助理兼容字段，避免影响已有派单链路。
- 已完成参与者默认不再占用助理，可被后续派单逻辑视为空闲。

### 评审

- `task_collaborators` 已新增参与者独立时间字段，并将旧 `active` 状态迁移为 `waiting`。
- 派单/指定助理会补 `primary` 参与记录；摄影师添加协作助理会新增 `helper` 参与记录。
- 助理开始、暂停、完成时会带 `actorAssistantId`，只更新自己的参与记录。
- 协作助理完成自己的部分后，该助理参与状态变为 `completed`，主任务仍保持未完成；所有参与者都完成后主任务才 `completed`。
- 摄影师任务卡片会显示参与者姓名与各自状态，助理任务列表按自己的参与状态显示。
- 已执行 `npx prisma db push`、`npx prisma generate` 和旧数据补齐 SQL。
- API 回归通过：主助理 + 协作助理分别开始/完成，验证“单人完成不完结、全员完成才完结”。
- `npx tsc --noEmit` 通过。
- 浏览器已刷新 `http://localhost:3001/photographer`，页面正常渲染。

## 调整：提权只作用于未分配队列任务

### 计划

- [x] 将动态提权扫描限制为 `waiting + assistantId=null` 的任务。
- [x] 后台逻辑设置文案明确为“未分配队列任务”。
- [x] 类型检查并刷新浏览器验证。

### 评审

- 后端 `escalatePriorities` 已增加 `assistantId: null` 条件，只对未派发任务提权。
- 已派给助理但还没开始的待就位任务，不再受“队列等待多久提权”滑杆影响。
- 后台文案已改为“未分配队列任务等待多久进行优先级提权”，并补充说明已派发待就位不参与普通队列提权。
- 回归验证通过：未分配 waiting 任务从 P4 提到 P3；已分配待就位 waiting 任务保持 P4。
- `npx tsc --noEmit` 通过。
- 浏览器后台「逻辑设置」已刷新，确认新文案显示。

## 新功能：按大区开关多人协作规则

### 计划

- [x] 在后台「逻辑设置」增加每个大区的「多人协作规则」开关。
- [x] 将开关保存到 `system_config`，并默认开启，避免影响已有协作功能。
- [x] 摄影师端按当前任务所在大区控制「添加协作 / 协作 N人」入口。
- [x] 后端协作接口按任务摄影师所在大区强校验，关闭时禁止新增协作，但允许移除已有协作成员。
- [x] 类型检查、API 回归和浏览器验证。

### 可选项

- 已存在协作成员的任务关闭规则后，保留状态展示，只隐藏/禁止新增入口。
- 空协作列表提交用于清理协作关系时继续允许，方便关闭规则后收口已有任务。

### 评审

- 后台「逻辑设置」已新增「多人协作规则」卡片，每个大区独立开关，保存到 `system_config` 的 `collaboration_enabled_b{buildingId}`。
- 开关默认开启；未配置的大区不会影响现有长时任务协作入口。
- 摄影师任务列表会按任务摄影师所在大区判断是否显示「添加协作 / 协作 N人」按钮；规则关闭后仍保留已有协作成员状态展示。
- `/api/tasks/[id]/collaborators` 已加入后端强校验：关闭大区时禁止新增协作助理，但允许提交空列表移除已有协作。
- API 回归通过：关闭新增协作返回 400，关闭后空列表返回 200，重新开启后新增返回 200。
- `npx tsc --noEmit` 通过；浏览器验证后台出现 5 个大区开关，摄影师工作台正常渲染。

## 调整：30分钟以上任务均可多人协作

### 计划

- [x] 将多人协作资格统一为任务类型 `maxDuration > 30`。
- [x] 前端协作入口和后端协作接口使用同一个资格判断。
- [x] 后台文案明确“30分钟以上任务”。
- [x] 类型检查、API 回归和浏览器验证。

### 评审

- 已新增共享规则 `taskCategoryAllowsCollaboration`，统一判断任务类型最长时长是否 `> 30`。
- 摄影师端「添加协作」入口和后端 `/api/tasks/[id]/collaborators` 都使用同一判断，避免前端可见但接口不认，或接口绕过前端规则。
- 后台「多人协作规则」说明已改为“30 分钟以上任务”。
- API 回归通过：15 分钟任务新增协作返回 400，60 分钟任务新增协作返回 200。
- `npx tsc --noEmit` 通过；浏览器验证后台文案和摄影师工作台渲染正常。

## Bug：30-60分钟任务不能添加协作助理

### 计划

- [x] 复现 30-60 分钟任务协作入口/保存失败的具体表现。
- [x] 核查前端入口、候选助理筛选和后端协作 API 的时长规则。
- [x] 修复 30 分钟以上任务协作规则不一致的问题。
- [x] 类型检查、API 回归和浏览器验证。

### 评审

- 根因之一：管理身份的右侧任务列表原来按 `photographerId=当前管理账号` 拉取，导致地图上能看到其它摄影师的 30-60 分钟任务，但任务列表里没有它，也就无法点「添加协作」。
- 已修复：管理身份现在拉取当前大区今日任务，可直接管理当前大区内 30 分钟以上任务的协作。
- 根因之二：`60分钟以上` 类型在数据库中是 `minDuration=60, maxDuration=0`，原规则只看 `maxDuration > 30`，导致 60 分钟以上未被识别为可协作。
- 已修复：多人协作资格改为 `maxDuration > 30` 或 `minDuration >= 30`，覆盖 30-60 和 60 分钟以上。
- API 回归通过：5-15 分钟任务新增协作返回 400，30-60 分钟返回 200，60 分钟以上返回 200。
- `npx tsc --noEmit` 通过；浏览器验证管理身份在当前大区任务列表中可看到协作入口/协作状态。

## 修正：协作助理暂停时地图保留暂停头像

### 计划

- [x] 定位地图头像与右侧 Dock 的助理状态合成逻辑。
- [x] 确认协作参与者 `paused` 状态当前被映射成什么展示状态。
- [x] 修复多人协作中单个助理暂停时的地图头像展示，保留在任务位置并显示暂停态。
- [x] 类型检查并用浏览器验证地图/Dock 展示。

### 评审

- 根因：协作助理的 `paused` 参与状态原来被合成为 `busy + currentRoom`，地图走主头像渲染路径，没有进入已有的暂停头像 `pausedRoom` 渲染路径。
- 已修复：当协作参与者状态为 `paused` 时，将该助理的任务位置写入 `pausedRoom`，并复用暂停灰头像、暂停 tooltip、超时描边逻辑。
- 暂停耗时/超时计算改为读取该助理自己的参与记录，避免多人协作时用整条任务的汇总时间。
- 已用临时多人协作任务验证数据路径，并清理临时任务与人员。
- `npx tsc --noEmit` 通过；浏览器刷新工作台无运行时错误，地图/Dock 正常渲染。

## Bug：摄影师切换场地后头像名片位置不更新

### 计划

- [x] 定位头像名片位置文本的数据来源。
- [x] 定位摄影师/助理切换场地按钮的状态更新逻辑。
- [x] 修复摄影师切换场地后本地 `profile.currentRoom` 与名片显示同步。
- [x] 类型检查并浏览器验证。

### 评审

- 根因一：头像名片位置对非助理身份优先显示 `originalRoomRef.current`，所以摄影师切换到额外场地后仍显示原始房间。
- 根因二：`switchVenue` 只有助理身份会 PATCH 保存到 `/api/profiles/:id`，摄影师切换位置没有持久化，刷新或资料重拉后会回到旧位置。
- 已修复：摄影师/管理身份名片优先显示 `profile.currentRoom`；助理身份保留原有逻辑。
- 已修复：摄影师切换场地也会保存到后端，并同步更新 `profile` 与 `allProfiles` 本地状态。
- `npx tsc --noEmit` 通过；浏览器刷新工作台无运行时错误；数据库 PATCH 持久化验证通过并已恢复测试数据。

## 新功能：摄影师跨楼座场地切换

### 计划

- [x] 梳理头像名片位置展示、楼座切换、公共区域切换的现有逻辑。
- [x] 将摄影师位置操作拆成楼座切换与场地切换两层。
- [x] 跨到其它楼座时只提供该楼座公共区域；切回人员登记楼座时允许切回办公室房间。
- [x] 保存切换结果并同步名片、快捷预约任务地点和地图大区。
- [x] 类型检查并浏览器验证。

### 评审

- 摄影师/管理身份的名片位置已拆成两个可点击区域：左侧楼座、右侧场地。
- 点击楼座会在右侧弹出其它楼座；切到其它楼座时默认进入该楼座第一个公共区域。
- 点击场地会在右侧弹出当前楼座的公共区域；只有当前楼座等于页面进入时的登记楼座时，才额外显示自己的办公室房间选项。
- 切换楼座会同步 `profile.buildingId/currentRoom`、地图当前大区、名片文本和后端数据；后续快捷预约会使用新场地。
- `npx tsc --noEmit` 通过；浏览器验证名片出现楼座/场地两个按钮，点击后分别出现「切换楼座」「切换场地」菜单。

## 调整：摄影师位置菜单交互细节

### 计划

- [x] 去掉名片楼座与场地文字下方的横线样式。
- [x] 恢复位置区域之前的动态悬停展开交互。
- [x] 扩大右侧菜单移入容错，避免鼠标移动时菜单过早收回。
- [x] 类型检查并浏览器验证。

### 评审

- 已移除楼座/场地文字按钮的下划线，改为轻量 hover 背景反馈。
- 楼座文字 hover/点击会打开「切换楼座」，场地文字 hover/点击会打开「切换场地」，恢复动态展开感。
- 右侧菜单透明桥接区域从 `24px` 扩到 `72px`，菜单位置从 `+24px` 调到 `+36px`，移入更宽容。
- 增加 `420ms` 延迟收起与移入取消关闭，减少鼠标移动到右侧菜单时误收回。
- `npx tsc --noEmit` 通过；浏览器验证两个位置按钮无下划线，点击分别出现「切换楼座」「切换场地」。

## 调整：位置菜单展开方向与菜单项动效

### 计划

- [x] 右侧场地菜单向左收紧约 50% 间距。
- [x] 场地菜单项每行首字符左对齐，并加入与任务列表一致的轻微 hover 动画。
- [x] 楼座切换菜单改为从楼座文字下方向下滑出，不再显示在右侧。
- [x] 楼座菜单使用白色半透明底框，菜单项左对齐并保留轻微 hover 动画。
- [x] 类型检查并浏览器验证。

### 评审

- 右侧「切换场地」菜单间距已从 `calc(100% + 36px)` 收紧到 `calc(100% + 18px)`，透明桥接区同步缩减，视觉上更贴近名片位置文字。
- 场地菜单项改为左对齐首字符，并加入 `hover:translate-x-1 hover:scale-[1.02] hover:shadow-md` 的轻微交互动效。
- 「切换楼座」不再走右侧弹出，改为从楼座文字下方滑出，使用白色半透明毛玻璃底框。
- 楼座菜单项同样保持左对齐，并带轻微位移、缩放和阴影 hover 反馈。
- `npx tsc --noEmit` 通过；浏览器验证楼座菜单从 `B座` 下方展开，场地菜单右侧间距为 18px，页面无运行时错误。

## 微调：场地菜单间距与宽度

### 计划

- [x] 将右侧场地菜单间距从过紧的 `18px` 调整为更自然的 `24px`。
- [x] 取消场地菜单固定最小宽度，改为按最长菜单项内容自适应。
- [x] 微调菜单项左右内边距，减少右侧多余空白。
- [x] 类型检查并浏览器验证。

### 评审

- 右侧场地菜单现在使用 `calc(100% + 24px)`，比上一版多留出一点呼吸感。
- 场地菜单和同类右侧滑出菜单从固定 `min-w` 改为 `w-max`，宽度按最长文字项自然匹配。
- 菜单项水平内边距从 `px-3` 收到 `px-2.5`，保留可点击面积，同时减少右侧空白。
- `npx tsc --noEmit` 通过；浏览器验证当前菜单宽度从 84px 收到 80px，左侧间距为 24px。

## 修正：楼座菜单遮挡与选项样式

### 计划

- [x] 提高头像名片面板和楼座下拉菜单层级，避免被下方任务面板遮挡。
- [x] 去掉楼座选项的蓝色背景块和阴影。
- [x] 将楼座选项改为纯黑文字选择，并保留轻微 hover 位移动效。
- [x] 类型检查并浏览器验证。

### 评审

- 头像名片面板增加 `relative z-[80] overflow-visible`，楼座下拉菜单提升到 `z-[200]`。
- 楼座菜单项已改为透明背景、黑色文字、无阴影；hover 时只做轻微右移和橙色文字反馈。
- 浏览器验证：楼座下拉菜单行在与下方区域重叠的位置仍为顶层元素，选项背景为透明，文字为黑色。
- `npx tsc --noEmit` 通过。

## 修正：楼座按钮一体化与跨区未选场地

### 计划

- [x] 将坐标图标与楼座文字合并为同一个可 hover/click 的切换入口。
- [x] 楼座下拉只显示其它楼座名称，移除标题行，宽度按楼座名称收窄。
- [x] 切换到非登记楼座时不再沿用原房间号或默认公共区域，场地字段显示「未选择」。
- [x] 增加历史脏数据防护：当前房间不属于当前楼座时也显示「未选择」。
- [x] 类型检查并浏览器验证。

### 评审

- 摄影师名片中的坐标图标和楼座文字现在属于同一个按钮，鼠标移入任意位置都会向下滑出楼座列表。
- 楼座下拉移除了「切换楼座」标题，只保留其它楼座选项；浏览器验证当前菜单宽度为 74px，选项为透明背景黑色文字。
- `switchPhotographerBuilding` 切到非登记楼座时会把 `currentRoom` 置为 `null`，右侧场地按钮显示「未选择」。
- 修正 `isAtRegisteredBuilding` 判断，避免摄影师在非登记楼座时继续回退显示原办公室房间。
- 新增 `venueBelongsToBuilding` 校验，刷新后如果历史数据里仍残留“其它楼座 + 原房间号”，也不会继续显示错误房间。
- `npx tsc --noEmit` 通过；浏览器验证当前 `B座 + S2` 历史状态已显示为 `B座 · 未选择`，楼座菜单只显示其它楼座，宽度为 74px。

## 微调：楼座下拉宽度与字号

### 计划

- [x] 缩小楼座下拉的展开范围，菜单按内容自然收缩。
- [x] 楼座选项按首字同一左边界排列。
- [x] 将楼座下拉字号调到与名片位置文字一致的 `13px`。
- [x] 类型检查并浏览器验证。

### 评审

- 楼座下拉容器从撑满行宽改为 `w-fit`，选项行改为 `w-auto`，不再产生明显右侧空白。
- 选项行统一左边界，浏览器验证各行首字 `left=122px`。
- 选项字号已调整为 `13px`，与名片位置文字一致。
- `npx tsc --noEmit` 通过；浏览器验证楼座菜单宽度为 74px，最长选项宽度为 64px。

## 微调：楼座下拉首字对齐名片文字

### 计划

- [x] 将楼座下拉左边缘从按钮右侧对齐改为楼座文字首字对齐。
- [x] 去掉菜单横向内边距，避免文字首字被右推。
- [x] 类型检查并浏览器验证。

### 评审

- 楼座下拉改为 `left-[22px]`，避开坐标图标宽度，起点对齐名片上的楼座文字。
- 菜单容器横向 padding 已取消，选项只保留右侧内边距和上下留白。
- `npx tsc --noEmit` 通过；浏览器验证名片楼座首字与下拉首行首字 `delta=0px`，菜单宽度收窄为 60px。

## 微调：楼座下拉底框留白与垂直距离

### 计划

- [x] 底框左侧恢复少量留白，同时保持文字首字对齐名片楼座文字。
- [x] 缩短楼座下拉与上方名片文字的垂直间距。
- [x] 类型检查并浏览器验证。

### 评审

- 楼座下拉容器从 `left-[22px]` 调为 `left-[18px]` 并加 `px-1`，底框左侧留出约 5px。
- 下拉垂直距离从 `mt-2` 收到 `mt-1`，浏览器验证与上方按钮间距为 4px。
- 名片楼座文字首字与下拉首行首字仍保持对齐，浏览器验证 `textDelta=0px`。
- `npx tsc --noEmit` 通过。

## 调整：多人协作完成任务展示

### 计划

- [x] 已完成的多人协作任务在人员位置显示主助理和协作助理姓名。
- [x] 三人及以上姓名使用 hover 横向滚动，展示到最后一个人名。
- [x] 右上角完成标签从「已完成」改为蓝色「多人协作完成」。
- [x] 类型检查并确认当前数据状态。

### 评审

- 新增 `taskAssigneeNames`，用于去重合并主助理与协作助理姓名。
- 任务卡片检测到 `已完成` 且存在协作助理时，第二行会显示多个助理姓名。
- 三人及以上使用 `.task-assignee-marquee`，鼠标移入后滚动到最后一个姓名；未溢出时不会反向移动。
- 多人协作完成标签使用蓝色样式 `bg-blue-100/70 text-blue-600`，文案为「多人协作完成」。
- 顶部任务标题增加截断保护，右上角状态标签固定不收缩，避免长标签挤压错位。
- `npx tsc --noEmit` 通过；当前接口中暂无“今日已完成且带协作助理”的任务样例，因此未做真实卡片截图验证。

## 修正：协作人数上限包含主助理

### 计划

- [x] 将后台设置的“最多 N 人”定义为任务总参与人数，包含自动派发的主助理。
- [x] 后端协作接口按“主助理 + 协作助理”总数校验上限。
- [x] 摄影师协作弹窗只允许选择剩余协作名额，超出时不选中并触发列表震动和红字提示。
- [x] 类型检查、接口回归和浏览器验证。

### 评审

- 后端 `/api/tasks/[id]/collaborators` 已按 `主助理 1 人 + 协作助理 N 人` 计算总参与人数；总数超过后台设置时返回 `超过多人协作 X 人上限`。
- 摄影师协作弹窗底部改为显示可额外选择人数，例如上限 3 且已有主助理时显示 `已选协作 0 / 可选 2 人（总上限 3 人）`。
- 达到可选协作名额后，再点击第 3 名协作助理不会选中该助理，只触发列表短暂震动和红色 `超过多人协作 3 人上限` 提示。
- `npx tsc --noEmit` 通过。
- API 回归通过：主助理 1 人 + 协作助理 3 人被 400 拦截；主助理 1 人 + 协作助理 2 人保存成功。临时任务与协作记录已清理。
- 浏览器验证通过：选满 2 名协作助理后尝试点击第 3 名，第三名仍显示 `空闲中`，红字和 `.collab-limit-shake` 震动类出现；验证后已恢复到刘军身份页面。

## 调整：公共队列展开面板

### 计划

- [x] 将任务面板展开入口从橙色下拉胶囊改为顶部居中的灰色双横线按钮。
- [x] 展开公共队列时让任务面板内容切换为公共队列，标题从「我的任务」变为「公共队列」。
- [x] 公共队列卡片左侧增加排名数字，前三名分别使用红、橙、黄。
- [x] 对等待时间提权上来的任务显示「提权」标识。
- [x] 类型检查并用浏览器验证当前页面效果。

### 评审

- 任务面板顶部已改为居中的灰色双横线展开按钮，替代原来的橙色下拉胶囊。
- 点击展开后，面板标题从「我的任务」切换为「公共队列」，内容区由公共队列任务填满；再次点击可收起回到「我的任务」。
- 公共队列卡片左侧显示排名数字，第一名红色、第二名橙色、第三名黄色，后续为灰色。
- `TaskFromAPI` 已补充 `escalatedAt` 字段，公共队列任务存在 `escalatedAt` 时显示红色「提权」标识。
- `npx tsc --noEmit` 通过；浏览器验证展开按钮、标题切换、排名数字正常。临时构造带 `escalatedAt` 的队列任务验证「提权」标识显示正常，随后已清理临时任务和人员。

## 修正：待就位让行不受任务类型不可插单限制

### 计划

- [x] 将“待就位让行”和“执行中插单”拆成两套判断语义。
- [x] 待就位让行只看优先级、锁定状态、是否已有让行子单，不再检查任务类型是否可插单。
- [x] 执行中插单继续严格检查后台“可插单 / 30分钟内不可插单 / 最大离场时间”配置。
- [x] 用短时不可插单任务构造回归，验证待就位仍可让行给 P1。
- [x] 类型检查并记录结果。

### 评审

- 已调整 `interruptWaitingPreempt`：待就位让行不再调用 `taskCategoryCanBeInterrupted`，并在注释里明确这条路径属于队列让行，不套用任务类型“可被插单/30分钟内不可插单/最大离场时间”限制。
- 执行中插单路径未放宽，仍通过 `taskCategoryCanBeInterrupted`、锁定状态、优先级和最大离场时间筛选。
- 回归验证 1：临时构造 `手持 5-15分钟 / canBeInterrupted=false` 的 P2 待就位任务，P1 新任务成功让行并记录 `parentTaskId`，原任务仍保持 `waiting`。
- 回归验证 2：临时构造同样短时不可插单任务为 `executing`，`canInterrupt(..., P1)` 返回 `false`。
- 临时楼座、临时人员、临时任务均已清理；`npx tsc --noEmit` 通过。

## 新功能：摄影师同时下达任务上限

### 计划

- [x] 在后台「逻辑设置」的全局参数中新增“摄影师最多同时下达任务数”选项，选项为 `1个 / 2个 / 3个 / 4个`。
- [x] 用 `system_config` 持久化该选项，默认值建议为 `1`，避免未配置时放宽现有派发行为。
- [x] 在调度层新增统一读取与校验方法，统计同一摄影师当前未完成任务数，状态范围为 `waiting / executing / paused`。
- [x] 在 `/api/tasks` 创建任务时先执行上限校验；达到上限时返回明确错误，不创建新任务，也不进入自动派发、待就位让行或执行中插单。
- [x] 在摄影师工作台处理创建失败响应，撤销乐观插入的临时任务，并给出可见错误提示。
- [x] 补充种子配置，保证新库初始化后有默认配置。
- [x] 运行类型检查，并用浏览器验证后台选项保存、达到上限时阻止下单、未达上限时正常派发。

### 可选项

- 上限是否按“今日未完成任务”统计；当前建议按所有未完成任务统计，和现有 `cleanupStaleTasks` 释放过期任务的策略保持一致。
- 达到上限时是否允许管理员后台强制创建；当前建议先不加例外，保持规则作用于整个派发入口。

### 评审

- 新增 `photographer_max_active_tasks` 全局配置，后台逻辑设置页提供 `1个 / 2个 / 3个 / 4个` 选项，当前数据库已写入默认 `1`。
- 派发入口 `POST /api/tasks` 在创建任务前校验摄影师未完成任务数，统计状态为 `waiting / executing / paused`。
- 达到上限时 API 返回 `409` 和 `PHOTOGRAPHER_ACTIVE_TASK_LIMIT_REACHED`，不会创建任务，也不会进入自动派发、待就位让行或执行中插单。
- 摄影师工作台创建失败时会移除乐观插入的临时任务，并显示错误浮层。
- 回归验证通过：已有 4 个未完成任务的摄影师在上限 1 时被拦截；上限临时调为 4 后，0 个未完成任务的摄影师可以创建任务，测试任务已删除。
- `npx tsc --noEmit` 通过。
- `npm run lint` 未通过，失败项为项目既有 lint 问题；本次新增代码未引入类型错误。

## 调整：任务上限控件改为浮层下拉

### 计划

- [x] 去掉“摄影师最多同时下达任务数”下方四个按钮。
- [x] 改为“最多 N 个”按钮 + 浮层选项的自定义下拉。
- [x] 保存下拉和插单策略单选时只做静默配置保存，不再触发后台页面整体刷新。
- [x] 类型检查并刷新浏览器验证控件。

### 评审

- “摄影师最多同时下达任务数”已改为“最多 N 个”按钮 + 浮层选项的自定义下拉。
- 选项为 `1个 / 2个 / 3个 / 4个`，当前选中项使用紫色高亮。
- 切换任务上限与切换插单策略均只静默保存配置，不再触发后台页面整体刷新。
- `npx tsc --noEmit` 通过。
- `npx eslint src/app/admin/TaskLogicTab.tsx src/lib/photographerTaskLimit.ts` 通过。
- `/admin` 返回 200，并已重新打开后台页面。

### 追加调整

- [x] 移除卡片底部的“最多 N 个”独立下拉入口。
- [x] 将自定义下拉替换到右上角数值标签位置。
- [x] 右上角标签尺寸缩小为紧凑按钮。
- [x] `npx tsc --noEmit` 通过。
- [x] `npx eslint src/app/admin/TaskLogicTab.tsx src/lib/photographerTaskLimit.ts` 通过。

## 调整：摄影师任务上限超限提醒抖动

### 计划

- [x] 复用协作人数超限的 `collab-limit-shake` 动画。
- [x] 当创建任务接口返回 `PHOTOGRAPHER_ACTIVE_TASK_LIMIT_REACHED` 时，让错误提示小窗抖动。
- [x] 类型检查并验证相关文件 lint。

### 评审

- 任务创建错误浮窗新增 `taskCreateLimitWarning` 状态。
- 当 `/api/tasks` 返回 `PHOTOGRAPHER_ACTIVE_TASK_LIMIT_REACHED` 时，错误浮窗会复用 `collab-limit-shake` 震动动画。
- 普通创建失败仍只显示错误浮窗，不触发任务上限震动。
- `npx tsc --noEmit` 通过。
- `npx eslint src/app/photographer/page.tsx` 仍未通过，失败项为该文件既有 lint 问题；本次新增逻辑未引入类型错误。

## 调整：摄影师超上限任务进入个人队列

### 计划

- [x] 将“超过任务发布上限”从拒绝创建改为创建摄影师个人队列任务。
- [x] 用 `isLocked + lockReason` 标记摄影师上限队列任务，不新增数据库字段。
- [x] 自动派发、空闲认领、全局扫描和动态提权跳过摄影师上限队列任务。
- [x] 当摄影师未完成任务数低于上限时，自动释放最早的队列任务进入正常派发队列。
- [x] 摄影师「我的任务」中将这类任务显示为灰色 `队列中`。
- [x] 队列任务备注区域显示 `当前摄影师已达到最大N条任务发布量，待上一任务完结后进入公共队列中排队`。
- [x] 类型检查、接口回归与前端验证。

### 评审

- 超过摄影师任务上限时，`POST /api/tasks` 不再拒绝创建；新任务会创建为 `waiting`，并标记 `isLocked=true`、`lockReason=photographer_active_task_limit_queue`、`assistantId=null`。
- 个人队列任务会保留在摄影师自己的任务列表数据中；前端识别该标记后显示灰色 `队列中` 标签，并在备注区域显示 `当前摄影师已达到最大N条任务发布量，待上一任务完结后进入公共队列中排队`。
- 公共队列展示已排除 `photographer_active_task_limit_queue`，该任务在摄影师手里任务数未低于上限前不会进入公共队列排序。
- `sweepWaitingTasks` 会先尝试释放符合条件的个人队列任务；当摄影师完成 1 条未完成任务后，最早的个人队列任务解锁并进入正常排序/派发。
- 回归验证通过：上限 2，已有 2 条执行中任务时创建第 3 条，第 3 条创建为个人队列且不在公共队列筛选结果中；完成其中 1 条后触发 sweep，第 3 条解锁并被正常派发。
- HTTP 回归验证通过：通过 `http://localhost:3002/api/tasks` 创建第 3 条返回 201；任务先保持个人队列锁定，完成 1 条未完成任务并触发 sweep 后解锁并进入正常派发。
- `npx tsc --noEmit` 通过。
- `npx eslint src/lib/photographerTaskLimit.ts src/lib/scheduler.ts src/app/api/tasks/route.ts src/app/admin/TaskLogicTab.tsx` 通过；`src/app/photographer/page.tsx` 单文件 lint 仍失败，失败项为既有 `setState in effect`、`<a>` 和 `<img>` 规则问题。

## 微调：队列中任务标签可取消

### 计划

- [x] 复用现有任务标签 hover 变「取消」的交互。
- [x] 将摄影师个人队列任务加入可取消条件。
- [x] 类型检查并确认页面可访问。

### 评审

- 队列中任务现在复用等待中/待就位任务的标签取消交互：鼠标移到右上角标签时显示红色「取消」，点击后调用现有 `/api/tasks/:id` 删除逻辑。
- `npx tsc --noEmit` 通过。
- 修改后曾确认 `/photographer` 返回 200；后续 dev server 进程自行退出，已单独重启确认。

## 调整：公共队列提权上调动效

### 计划

- [x] 保留现有后台 `upgrade_threshold` 提权规则：只作用于 `waiting + assistantId=null` 的公共队列任务。
- [x] 确认公共队列排序继续按提权后的 `priority` 排序，并保留红色「提权」标识。
- [x] 在前端为每个用户记录公共队列上一轮可见排序位置，打开公共队列时先展示旧位置。
- [x] 检测带 `escalatedAt` 且排名上升、当前用户尚未看过的任务，生成待播放上调队列。
- [x] 按顺序逐个播放“上调插入”动画：先保持旧排序，再将当前任务插入到新位置并高亮。
- [x] 用本地记录保存“用户已看过某任务某次提权”，同一个人再次打开公共队列不重复播放。
- [x] 对排名上升的公共队列卡片添加一次性“上调插入”动画，表现为轻微上浮、插入高亮、边框/阴影闪烁后回落。
- [x] 避免首次加载全部卡片误触发动效；没有上一轮位置时只记录基线，不播放。
- [x] 类型检查，并确认页面与任务接口可访问。

### 可选项

- 动效只给真正排名上升的卡片，而不是所有带「提权」标识的卡片。
- 动效持续约 700-900ms，避免影响公共队列滚动和点击。
- “已看过”按当前登录/切换后的 profile 维度保存在浏览器 `localStorage`，不新增数据库字段。

### 评审

- 后端提权规则未改，仍由 `/api/tasks` 查询时触发 `escalatePriorities`，按后台设置的 `upgrade_threshold` 处理未分配公共队列任务。
- 公共队列继续按提权后的 `priority` 排序，已有红色「提权」标识保留。
- 前端新增公共队列视觉排序层：发现当前 profile 尚未看过的 `escalatedAt` 任务时，先用“提权前优先级”计算旧位置，再按最终位置逐个插入上调。
- 已看过记录保存在 `localStorage` 的 `publicQueueEscalationSeen:<profileId>`，同一用户同一次提权不会重复播放。
- 上调动画为一次性 `public-queue-promote-inserting`，包含上浮、橙色高亮、边框和阴影反馈。
- 修正触发条件过窄的问题：现在会优先使用当前身份上次看到的公共队列顺序；没有旧顺序时会构造一个可见的提权前位置，避免 P1 或同级排序下没有位移而不播放。
- 本地记录升级为 `publicQueueEscalationSeen:v2:<profileId>` 和 `publicQueueLastOrder:v2:<profileId>:<buildingId>`，避免沿用旧版误标记的“已看过”状态。
- `npx tsc --noEmit` 通过。
- `http://localhost:3000/photographer` 返回 200，`/api/tasks?todayOnly=true` 返回 JSON；当前数据存在 2 条已提权公共队列任务，可用于切换到对应区域后验证动效。

## 微调：公共队列待就位超时标识

### 计划

- [x] 读取后台 `upgrade_threshold` 作为公共队列等待超时阈值。
- [x] 真正提权的未分配任务继续显示红色「提权」。
- [x] 已派发待就位任务如果等待超过阈值且未真正提权，则显示橙色「超时等待」。
- [x] 类型检查并用当前接口数据验证命中数量。

### 评审

- 公共队列卡片新增「超时等待」标识，适用于 `waiting + 已分配助理 + 未 escalatedAt + 等待分钟数 >= upgrade_threshold`。
- 当前后台阈值为 25 分钟；当前数据中有 2 条待就位超时任务会命中新标签。
- `npx tsc --noEmit` 通过。
- `http://localhost:3000/photographer` 返回 200。

## 设计探索：地图/队列双标签大容器

### 计划

- [x] 基于现有摄影师页截图目标，设计 3 个中间大容器方案：地图优先、队列优先、双态工作台。
- [x] 复用项目楼层图和头像素材，生成可直接查看的静态设计稿。
- [x] 将 3 版设计稿与总览入口放到 `~/Downloads`。
- [x] 导出预览图，便于直接对比。

### 评审

- 方案一“地图主控版”：地图作为大容器主体，队列任务做右侧浮层，最接近当前工作流，落地风险最低。
- 方案二“队列指挥台版”：队列任务作为主界面，地图缩成上下文卡片，最适合展示提权、等待、个人队列和协作关闭规则。
- 方案三“沉浸双态版”：地图始终在底层，队列从底部抽屉进入，动效和整体感最好，但实现成本最高。
- 设计稿已放到 `/Users/ljuuuu/Downloads/photographer-redesign-concepts/`。
- 已生成总览 HTML、3 个单版 HTML，以及对应 PNG 预览图。

## 微调：完成协作标签与房间助理对齐

### 计划

- [x] 多人协作完成标签统一使用“已完成”绿色状态样式。
- [x] 修正完成协作卡片中房间号与助理姓名的垂直对齐。
- [x] 类型检查与页面可访问验证。

### 评审

- “多人协作完成”标签已复用 `STATUS_STYLE.completed.tagCls`，与“已完成”保持同一绿色样式。
- 完成协作姓名行新增专用 `task-assignee-line` / `task-assignee-static` 对齐样式，避免被通用跑马灯 20px 行高拉偏。
- `npx tsc --noEmit` 通过。
- `http://localhost:3000/photographer` 返回 200。

## 调整：提权显示初始等级来源

### 计划

- [x] 在任务表记录第一次提权前的原始优先级。
- [x] 提权逻辑第一次写入原始优先级，后续继续提权不覆盖。
- [x] API 返回原始优先级字段，前端类型补齐。
- [x] 公共队列将提权标识从「提权」升级为 `P初始→P当前`。
- [x] 摄影师与助理「我的任务」列表也按同样方式显示提权来源。
- [x] 迁移数据库、生成 Prisma 类型、类型检查并回归。

### 评审

- `BookingTask` 新增 `escalatedFromPriority`，映射到数据库列 `escalated_from_priority`。
- `escalatePriorities()` 第一次提权时记录原始 priority，后续继续提权只更新当前 priority 和 `escalatedAt`，不覆盖初始来源。
- 前端 `TaskFromAPI` 补充 `escalatedFromPriority`，公共队列和摄影师/助理「我的任务」列表都优先显示 `P初始→P当前`；旧数据没有来源时回退显示「提权」。
- 已执行 `npx prisma db push` 和 `npx prisma generate`。
- 验证中误触发过 5 条真实队列任务提权，已立即按 `escalatedFromPriority` 恢复原 priority，并清空测试写入的提权字段；复查最近 15 分钟提权记录为 0。
- `npx tsc --noEmit` 通过，`http://localhost:3000/photographer` 返回 200。

## 微调：我的任务列表显示优先级

### 计划

- [x] 在摄影师/助理「我的任务」卡片标题前增加 P 等级标识。
- [x] 复用公共队列 P 等级颜色，保持 P1-P5 视觉一致。
- [x] 类型检查并确认页面可访问。

### 评审

- 摄影师和助理「我的任务」卡片标题前已增加 `P1`-`P5` 等级标识。
- 标识复用公共队列 P 等级颜色，和公共队列视觉保持一致；P4 使用蓝色等级样式，P5 保持灰色。
- `npx tsc --noEmit` 通过，`http://localhost:3000/photographer` 返回 200。

## 微调：多人协作规则说明布局

### 计划

- [x] 将自动关闭阈值按钮文案改为“队列任务达 N 条自动关闭多人协作逻辑”。
- [x] 将说明文字从单行截断改为两行完整展示。
- [x] 将阈值字段移到说明区域右侧，只让“N条”作为可选下拉，其余字段固定展示。
- [x] 将阈值字段移到大区列表下方，顶部说明恢复为单排短文案。
- [x] 类型检查并确认后台页面可访问。

### 评审

- 多人协作规则阈值字段已移到大区列表下方，固定展示“队列任务达 / 自动关闭多人协作逻辑”，仅“N条”为下拉可选项。
- 顶部说明恢复为单排短文案：说明按大区控制 30 分钟以上任务协作、队列达阈值后关闭新增协作，已有协作仍可展示和移除。
- `npx tsc --noEmit` 通过，`http://localhost:3000/admin` 返回 200。

## 微调：摄影师工作台左侧面板加宽

### 计划

- [x] 将左侧名片、快捷预约、我的任务三块容器统一加宽 10%。
- [x] 同步右移顶部区域切换和底部图例的左边界，避免与新宽度冲突。
- [x] 类型检查并确认工作台页面可访问。

### 评审

- 左侧总容器宽度从 `250px` 调整为 `275px`，三块面板保持一致宽度。
- 中间顶部区域切换和底部图例左边界从 `268px` 同步调整为 `293px`。
- `npx tsc --noEmit` 通过，`http://localhost:3000/photographer` 返回 200。

## 调整：我的任务与公共队列切换入口

### 计划

- [x] 移除任务面板顶部双横线折叠按钮。
- [x] 标题只展示当前面板名称，另一个面板名称折叠在标题下方。
- [x] 鼠标移到标题时显示另一个面板入口，点击后切换。
- [x] 类型检查并确认工作台页面可访问。

### 评审

- 任务面板不再显示顶部双横线按钮。
- 当前标题显示「我的任务」或「公共队列」；鼠标 hover 标题时，下方浮出另一个入口，点击切换面板。
- `npx tsc --noEmit` 通过，`http://localhost:3000/photographer` 返回 200。

## Bug：摄影师切换到跨楼座公共区域后下单显示 418

### 计划

- [x] 定位摄影师跨楼座/场地切换后的 `currentRoom` 写入路径。
- [x] 定位快捷下单时任务房间字段的来源。
- [x] 修复跨楼座默认场地与下单房间兜底逻辑。
- [x] 类型检查并记录结果。

### 评审

- 根因：摄影师跨楼座切换时 `currentRoom` 被设为 `null`；快捷下单又用 `profile.currentRoom || "418"` 作为任务房间，所以当前位置为空时任务固定落到 418。
- 已修复：摄影师切到其它楼座时默认写入该楼座默认公共区域；当前 B 座配置的默认公共区域为「一楼区域」。
- 已修复：快捷下单移除硬编码 418，当前位置为空时只退到当前楼座默认场地；若当前楼座没有任何可用场地，则直接提示无法创建任务。
- `npx tsc --noEmit` 通过。

## 调整：摄影师工作台位置切换不覆盖人员管理登记

### 计划

- [x] 将摄影师/管理身份的工作台位置切换限定为前端临时状态，不再 PATCH `profiles.buildingId/currentRoom`。
- [x] 保留助理/助理组长的当前服务区域逻辑，但不覆盖人员管理登记字段。
- [x] 确认快捷下单继续使用工作台临时位置作为任务地点。
- [x] 类型检查并记录结果。

### 可选项

- 是否需要我顺手把徐建玲的人员管理登记房间恢复成原值；需要你确认原本登记的是哪个楼座和房间。

### 评审

- 已由「工作台临时位置与人员登记位置分离」统一实现：摄影师临时位置不落库；助理写入独立当前服务区域字段；任务使用独立发生楼座派单。

## Bug：助理切换楼座后头像名片位置不更新

### 计划

- [x] 定位助理切换楼座后的 `currentRoom` 和 `buildingId` 更新逻辑。
- [x] 定位头像名片地理位置展示的数据来源。
- [x] 修复助理跨楼座默认场地和本地人员列表同步。
- [x] 类型检查并记录结果。

### 评审

- 根因：助理跨到其它楼座时 `currentRoom` 仍被设为 `null`，头像名片只能显示楼座或被旧任务位置覆盖；同时切换成功后没有同步 `allProfiles` 本地人员列表，地图/头像相关数据可能继续使用旧楼座信息。
- 已修复：助理跨楼座时默认写入目标楼座默认场地，例如 B 座会落到「一楼区域」。
- 已修复：助理切换成功后同步更新 `profile` 与 `allProfiles` 的楼座、场地和状态。
- `npx tsc --noEmit` 通过。

## Bug：助理楼座菜单没有排除当前名片区域

### 计划

- [x] 定位助理头像名片当前区域展示来源。
- [x] 定位助理可选楼座列表过滤条件。
- [x] 将助理可选楼座改为排除名片当前展示区域，而不是只排除 `profile.buildingId`。
- [x] 类型检查并记录结果。

### 评审

- 根因：助理名片展示的是当前任务所在楼座时，可选楼座仍按 `profile.buildingId` 过滤；当两者不一致，就会把名片当前楼座放进菜单，并漏掉真正剩余的楼座。
- 已修复：新增 `assistantDisplayedBuildingId`，优先取名片当前任务楼座，否则取个人当前楼座。
- 已修复：助理楼座菜单按 `assistantDisplayedBuildingId` 排除当前区域，列表展示为剩余区域。
- `npx tsc --noEmit` 通过。

## 调整：工作台临时位置与人员登记位置分离

### 计划

- [x] 梳理摄影师/助理头像名片位置切换、身份切换窗口分组、任务派发楼座的数据来源。
- [x] 给任务增加独立发生楼座字段，派单/协作/队列按任务发生楼座，不再借用摄影师登记楼座。
- [x] 摄影师工作台切换楼座/场地只改本页临时位置，不 PATCH 人员管理，也不移动身份切换窗口里的摄影师头像分组。
- [x] 助理切换楼座继续更新当前服务区域，并移动身份切换窗口里的助理头像分组；执行中名片展示任务地点。
- [x] 类型检查、数据库同步并记录结果。

### 评审

- 新增 `booking_tasks.location_building_id`：任务发生楼座独立于摄影师登记楼座；下单时写入工作台当前楼座。
- 新增 `profiles.active_building_id / active_room`：助理当前服务区域独立于人员管理里的登记楼座/办公室。
- 摄影师工作台楼座/场地切换不再调用 `/api/profiles/:id`，只更新本页临时位置；身份切换窗口仍按人员管理登记楼座分组摄影师。
- 助理切换楼座写入当前服务区域字段，派单、身份切换窗口助理分组、地图助理筛选按当前服务区域走；登记楼座/办公室不变。
- 任务派单、等待任务扫描、待就位让行、多人协作候选和公共队列均改为优先使用任务发生楼座。
- `npx prisma db push` 和 `npx prisma generate` 已执行；数据库已有新列。
- `npx tsc --noEmit` 通过。

## Bug：后台人员编辑保存失败无提示

### 计划

- [x] 定位人员编辑弹窗提交与 `/api/profiles/:id` 更新链路。
- [x] 修复保存失败时仍关闭弹窗、且不提示错误的问题。
- [x] 修正人员管理编辑登记楼座时误触发助理当前服务区域变更判断。
- [x] 类型检查并记录结果。

### 评审

- 根因一：人员编辑弹窗不检查 `res.ok`，接口失败时仍调用 `onSaved` 并关闭弹窗，导致用户看起来像“点了保存但没改”。
- 根因二：新增当前服务区域后，后端把普通 `buildingId` 登记楼座更新也纳入助理“服务区域切换”判断，可能误触发任务中禁止切楼座逻辑。
- 已修复：保存失败时弹窗不关闭，并显示接口返回错误。
- 已修复：普通人员管理编辑只更新登记字段；只有传入 `activeBuildingId` 才按助理当前服务区域切换处理。
- 已修复：空工号、空部门、空小组、空房间会规范为 `null`，避免空字符串触发唯一索引问题。
- `npx tsc --noEmit` 通过。

## 调整：摄影师超过发布上限后的个人队列

### 计划

- [x] 定位超过摄影师最大下发任务后的队列创建逻辑。
- [x] 限制同一摄影师最多只保留 1 条超过上限后的个人队列任务；再继续创建则拒绝并提示原因。
- [x] 将这类个人队列任务排到「我的任务」最上方。
- [x] 将队列原因备注改为红色警示文字，图标改为黄色三角感叹号。
- [x] 修复备注滚动距离，只滚动到最后一个文字。
- [x] 类型检查并记录结果。

### 评审

- 后端：当摄影师达到最大下发任务数时，只允许创建 1 条 `photographer_active_task_limit_queue` 个人队列任务；已有 1 条后继续下发返回 `409 / PHOTOGRAPHER_LIMIT_QUEUE_FULL`，不再创建新任务。
- 前端：`队列中` 状态排序提升到「我的任务」最上方。
- 前端：个人队列原因备注改为红色警示文字，左侧图标从气泡改为黄色三角感叹号。
- 前端：备注横向滚动改为按 `scrollWidth - clientWidth` 计算，只滚到最后一个字，不再整段滚出空白。
- 回归验证通过：最大下发数临时设为 2，已有 2 条未完成时第 3 条创建为队列任务，第四条返回 409 且未创建；临时数据已清理。
- `npx tsc --noEmit` 通过。

## Bug：我的任务看不到提权标识

### 计划

- [x] 确认当前提权标识只在公共队列渲染的位置。
- [x] 在「我的任务」卡片标题行补充 `escalatedAt` 对应的红色「提权」标识。
- [x] 类型检查并记录结果。

### 评审

- 根因：前端已有 `escalatedAt` 字段，但红色「提权」标识只渲染在公共队列；任务被自动派出后进入「我的任务」列表就看不到标识。
- 已修复：「我的任务」卡片标题行存在 `escalatedAt` 时显示红色「提权」标识。
- 当前本地数据库只有 27 条已完成历史任务带 `escalated_at`，没有未完成提权任务；因此现在页面上若没有新触发提权任务，仍不会出现标识。
- `npx tsc --noEmit` 通过。

## 调整：已完成任务不展示提权标识

### 计划

- [x] 将「我的任务」提权标识限定为未完成任务。
- [x] 确认进行中、队列中、待就位等未完成状态仍会显示提权标识。
- [x] 类型检查并记录结果。

### 评审

- 已完成任务即使有 `escalatedAt`，也不显示「提权」标识。
- 进行中、待就位、队列中等未完成任务存在 `escalatedAt` 时继续显示红色「提权」标识。
- 已用临时演示任务验证：队列中、待就位、进行中均显示「提权」，已完成任务不显示。
- `npx tsc --noEmit` 通过。

## Bug：手持 5-15 分钟提权后显示 P3

### 计划

- [x] 核查异常任务的任务类型优先级与任务动态优先级。
- [x] 修正临时演示数据：手持 5-15 分钟原始 P2，提权后动态优先级应为 P1。
- [x] 加固任务创建接口，基础优先级以任务类型配置为准，不信任前端传入的独立优先级。
- [x] 类型检查并记录结果。

### 评审

- 截图中的 P3 来自 `codex-demo-escalated-*` 临时演示数据手工写错，不是正常创建链路。
- 已修正演示数据：手持 5-15 分钟分类基础优先级为 P2，提权后动态优先级为 P1。
- `/api/tasks` 创建任务时已改为始终使用任务类型配置的 `priorityLevel` 作为初始动态优先级，不再信任前端传入的独立 `priority`。
- `npx tsc --noEmit` 通过。

## 调整：个人超额队列释放后重算公共排队时间

### 计划

- [x] 核查个人超额队列释放逻辑与公共队列等待时间来源。
- [x] 释放个人超额任务进入公共队列时，将公共排队起算时间重置为释放时间。
- [x] 确认公共队列排序、已等待文案和提权阈值都从释放时间开始计算。
- [x] 类型检查并记录结果。

### 评审

- 根因：个人超额任务释放时此前只清除锁定标记，保留了原始 `createdAt`；公共队列排序、卡片「已等待」和自动提权阈值都使用 `createdAt`，所以释放后会带着个人队列阶段的等待时间进入公共队列。
- 已修复：释放个人超额队列任务时同步刷新 `createdAt` 为释放时刻，并清空可能残留的 `escalatedAt`。
- 释放顺序仍按个人队列内原始创建时间最早优先，选中释放后才重置公共排队起算时间。
- 临时验证通过：构造 90 分钟前创建的个人超额队列任务，触发释放后 `is_locked=0`、`lock_reason=null`、`created_at` 更新为释放时刻、`escalated_at=null`。
- 已清理 `codex-demo-escalated-*` 演示任务，避免影响真实队列判断。
- `npx tsc --noEmit` 通过。

## 调整：暗夜模式地图整体提亮

### 计划

- [x] 定位暗夜模式地图底图滤镜与遮罩。
- [x] 将暗夜模式平面图从高反相压暗改为整体提亮、保留面域灰度。
- [x] 类型检查并用暗夜模式截图复核效果。

### 评审

- 根因：暗夜模式平面图使用 `invert(1) brightness(0.68) contrast(1.18)` 并叠加深色遮罩，白色面域被压到接近纯黑，只剩边缘线可见。
- 已调整为低反相、高亮度、低对比滤镜，并移除压暗遮罩，让房间和走廊面域保留深灰层级。
- 暗夜模式截图 `/tmp/dark-map-brightened-v2.png` 已复核：地图区域整体更亮，不再只有边缘线条。
- `npx tsc --noEmit` 通过。

## 新功能：队列拥堵时自动关闭多人协作

### 计划

- [x] 在后台逻辑管理新增“当前区域任务队列达到 N 条自动关闭多人协作”配置，选项为 4-10 条。
- [x] 新增统一配置解析与公共队列计数口径：按当前楼座公共队列 `waiting/paused`，排除摄影师个人超额队列。
- [x] 后端协作保存接口按该阈值拒绝新增协作，保留已有协作的展示和移除。
- [x] 工作台隐藏新增协作入口，并在协作弹窗中阻止队列拥堵时新增协作。
- [x] 类型检查与接口回归验证。

### 评审

- 已新增全局配置 `collaboration_queue_auto_close_limit`，后台逻辑设置支持在 4-10 条之间选择。
- 统一公共队列口径：统计当前楼座 `waiting/paused` 任务，排除 `photographer_active_task_limit_queue` 个人超额队列。
- 后端 `/api/tasks/[id]/collaborators` 在新增协作时读取楼座协作可用性；队列达到阈值返回 `400 / COLLABORATION_AUTO_CLOSED_BY_QUEUE`，手动关闭仍返回 `COLLABORATION_DISABLED_FOR_BUILDING`。
- 已有协作不受自动关闭影响：前端仍可打开已有协作名单并移除，后端保存时会保留请求中已有的协作人员，不会因为在线状态或楼座变化误删。
- 工作台任务卡片在队列达到阈值时不再展示“添加协作”入口；弹窗内只保留已有协作人员，并用红字提示当前区域队列已达到阈值。
- 协作候选助理按当前服务楼座判断，和后端 `activeBuildingId ?? buildingId` 口径一致。
- 接口回归通过：临时楼座阈值设为 4，队列达到 4 条时新增协作返回 `COLLABORATION_AUTO_CLOSED_BY_QUEUE`，移除已有协作返回 200；临时数据已清理。
- `npx tsc --noEmit` 通过。
- 浏览器已重新打开 `http://localhost:3000/admin`，逻辑设置页能看到“队列任务达 N 条自动关闭多人协作逻辑”；`/admin` 与 `/photographer` 均返回 200。
- `npx eslint ...` 在 `src/app/photographer/page.tsx` 的既有 lint 问题上失败：同步 effect setState、根链接 `<a>`、已有 `<img>` 使用等，不是本次逻辑新增导致。

## 新功能：待就位超时自动更换空闲助理

### 计划

- [x] 明确待就位超时阈值口径：默认复用后台 `upgrade_threshold`，也就是当前公共队列“超时等待”同一套分钟数；超过这个时间仍未开始任务，就认为原助理长时间未应答就位。
- [x] 在任务/通知数据层增加可持久化的换人通知记录，记录原助理、新助理、任务、原因、是否已确认。
- [x] 在调度层新增“待就位超时换人”步骤：当同楼座有新的空闲在线助理释放出来时，优先扫描已派发但未开始的 `waiting + assistantId != null` 任务；若原助理待就位超过阈值，则把任务主助理更换为空闲助理。
- [x] 超时待就位换人排序：先筛出同楼座已超时待就位任务，再按“任务等级权重 + 已等待时长”排序；等级越高、等待越久，越优先接替。
- [x] 换人事务中同步更新任务主助理与参与人记录：新助理成为 primary/waiting；原助理对应参与记录标记 left；任务备注 `note` 保持不变，让新助理继续收到摄影师备注提醒。
- [x] 换人事务中把原助理切为离线状态，并确保后续派单不会再选中该助理；新助理状态切为待就位。
- [x] 在助理工作台增加中心弹窗：新助理显示“因 xxx 长时间未应答就位，现由你接替派发任务”，点击确认后关闭；若任务带备注，继续显示现有摄影师备注确认弹窗。
- [x] 在被更换助理工作台增加中心弹窗：提示“你因长时间未应答就位，现已将你状态切换为离线状态，点击下方确认窗口，状态切换为应接在线状态。”下方提供确认按钮；确认后调用接口切回在线/空闲，并触发等待队列重新扫描。
- [x] 补充接口回归：构造待就位超时任务和新释放空闲助理，验证任务换派、原助理离线、两端通知、备注继承、确认后恢复在线。
- [x] 类型检查、必要的 Prisma 同步，并记录评审结果。

### 可选项

- 若不想复用 `upgrade_threshold`，可以在后台逻辑设置新增单独的“待就位未响应换人时间”，例如 5-60 分钟。
- 换人优先级放在普通未分配任务派发之前：新释放的空闲助理会先接替已经超时的待就位任务，再去接新的公共队列任务。

### 评审

- 新增 `standby_reassignment_notices` 表，记录换人任务、原助理、新助理、等待分钟、阈值、综合分和两端确认状态。
- `sweepWaitingTasks()` 开头优先执行超时待就位换人，再处理普通未分配任务；符合“超时待就位接替优先于普通派发”。
- 综合分公式为 `任务等级分 + 超时等待分`，其中任务等级分为 `(6 - P等级) × 阈值分钟`，超时等待分只计算超过阈值的等待分钟。
- 换人事务会把任务主助理切到新空闲助理，原 primary 参与记录标记为 `left`，新助理写入 primary/waiting，任务备注保持不变。
- 被替换助理会被切为 `offline + idle + isOnline=false`，确认提醒后恢复为 `online + idle` 并触发等待队列扫描。
- 助理工作台已新增两类中心弹窗：新助理接替提醒、旧助理离线恢复提醒；接替提醒确认后，若任务带备注，会继续出现原有摄影师备注弹窗。
- 已执行 `npx prisma db push`、`npx prisma generate`。
- 隔离回归通过：临时楼座构造超时待就位任务后，`reassignOverdueStandbyTasks()` 返回 1，任务换派给新助理，原助理离线，通知记录生成，备注保留；临时数据已清理并恢复 `upgrade_threshold=25`。
- `npx tsc --noEmit` 通过。

## Bug：同一助理连续叠加多个临时接派任务

### 计划

- [x] 核查黄钦当前任务链路，确认连续派单来自执行中插单还是待就位让行。
- [x] 执行中插单前检查该助理是否已有未完成的临时接派任务；有则不再继续给同一助理叠新插单。
- [x] 待就位让行同样检查同一助理是否已有任何活跃插单，避免不同父任务路径继续叠单。
- [x] 待就位让行补回“原任务允许离场上限”校验：新派任务最长时长必须小于等于原任务的可让行最大时间。
- [x] 统一使用新任务最大时长作为保守判断；例如新任务是 10-30 分钟，就按 30 分钟判断。
- [x] 类型检查与必要接口回归，记录评审结果。

### 评审

- 根因：黄钦当前 1 个执行中父任务下面已经挂了 3 个 waiting 插单；执行中插单路径只检查优先级和离场上限，没有检查同一助理是否已经有未完成插单。
- 另一个缺口：待就位让行路径此前注释明确“不套用最大离场时间限制”，会让新任务绕过原任务的可让行最大时间。
- 已修复：`canInterrupt()`、`interruptAssistant()` 和创建任务时的执行中插单候选都会排除已有 `parentTaskId != null` 活跃任务的助理，避免同一助理连续叠加多个临时接派任务。
- 已修复：待就位让行只允许该助理当前只有一个活跃待就位父任务，且没有任何活跃插单时才可被抢占。
- 已修复：待就位让行也会用新任务最长时长对比原任务允许离场上限；10-30 分钟按 30 分钟判断。
- 已修复：执行中插单候选按 `activeBuildingId ?? buildingId` 判断当前服务楼座，避免助理切换服务区域后派发口径不一致。
- 回归验证通过：黄钦已有 pending 插单时 `canInterrupt(..., P1, 5)` 返回 `false`。
- 回归验证通过：临时构造“原任务可让行 25 分钟”场景，20 分钟新单允许，30 分钟新单拒绝。
- 回归验证通过：临时构造待就位让行场景，30 分钟新单被拒绝，20 分钟新单可挂到父任务；临时数据已清理。
- 已修复当前黄钦异常数据：先保留最早一条待处理插单，后两条从黄钦身上摘回公共队列，相关参与记录标记为 `left`；页面轮询和现场操作后，黄钦当前只剩原执行中任务，当前不存在任何助理挂多条活跃插单。
- `npx tsc --noEmit` 通过。

## 调整：助理长时任务可添加协作助理

### 计划

- [x] 记录多人协作主副助理规则：第一个派发助理为主助理，主/副助理独立就位、开始、完成和工作时长。
- [x] 修改任务整体完成判定：所有未离开的主/副助理都完成后，任务才算完成；单个助理完成只释放个人。
- [x] 修改插单候选逻辑：多人协作任务中未完成的主/副助理不能被紧急单插单。
- [x] 后端协作接口允许摄影师或当前主助理维护协作名单，协作助理不能继续拉人。
- [x] 助理「我的任务」长时主任务卡片展示“添加协作/协作 N人”入口，复用摄影师协作弹窗与后台限制。
- [x] 回归验证：主副助理独立完成、任务聚合完成、协作中禁止插单、助理端添加协作。

### 评审

- 规则已写入长期记忆：`20260531-232630-assistant-collaboration-main-helper-rules.md`。
- 后端协作接口现在接收 `actorAssistantId`；摄影师仍可维护协作名单，助理只有当前任务主助理可维护，副助理继续拉人会返回 403。
- 新增协作候选后端校验：必须是同任务区域、在线、空闲、无活跃主任务、无活跃协作参与记录的助理。
- 助理「我的任务」里，长时主任务可显示“添加协作 / 协作 N人”入口；协作弹窗继续复用摄影师端人数上限、区域协作开关、队列拥堵自动关闭逻辑。
- 任务完成判定保持按 `TaskCollaborator` 聚合：主/副助理各自完成只释放个人，所有未离开的参与助理都完成后任务才整体完成。
- 插单候选已排除多人协作任务：执行中插单和待就位让行都不会抢占仍有活跃副助理的任务；副助理本身也因活跃参与记录不会进入派发/插单候选池。
- 回归通过：临时长时任务由主助理添加副助理成功；主助理先完成后任务仍未完成且主助理变空闲；副助理完成后任务整体完成。
- 回归通过：多人协作进行中主助理 `canInterrupt` 返回 `false`；副助理尝试维护协作名单返回 403。
- 临时回归数据已清理，`npx tsc --noEmit` 通过。

## 微调：任务面板切换入口与协作完成卡片

### 计划

- [x] 标题切换入口下拉项与当前标题左对齐，鼠标移入时向下滑出并带弹簧感。
- [x] 多人协作完成后隐藏卡片最底部协作信息。
- [x] 调整房间与助理信息行布局，让左侧房间/助理和右侧用时垂直对齐。
- [x] 类型检查与页面可访问验证。

### 评审

- 任务面板标题下拉已改为常驻渲染，通过透明度、纵向位移和缩放实现 hover 向下滑出的弹簧动效，并保持左对齐。
- 多人协作完成卡片不再显示底部协作人数/协作成员信息。
- 房间/助理信息行改为左右两列布局，右侧实际用时固定右对齐，完成协作人员姓名行使用 14px 行高避免顶偏。
- `npx tsc --noEmit` 通过。
- `http://localhost:3000/photographer` 返回 200。
